@startuml
title AS-IS модель процесса анализа рыночных данных

start

:Выбор актива;

partition "Внешние сервисы" {
  :Получение данных из CoinMarketCap;
  :Получение данных из TradingView;
}

if (Нужны дополнительные данные?) then (Да)
  partition "Внешние сервисы" {
    :Получение данных из CoinGecko;
  }
else (Нет)
endif

partition "Пользователь" {
  :Сопоставление данных вручную;
  note right
    Отсутствует единая система
    и централизованная обработка
  end note

  :Анализ данных вручную;
  note right
    Требуется переключение
    между несколькими сервисами
  end note
}

if (Информации достаточно?) then (Да)
  :Принятие решения;
else (Нет)
  :Повторный поиск данных;
  :Повторное сопоставление данных;
  :Принятие решения;
endif

stop
@enduml