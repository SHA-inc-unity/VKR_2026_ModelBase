@startuml
title AS-IS модель процесса анализа рыночных данных

start

:Выбор актива пользователем;

:Получение данных из CoinMarketCap;
:Получение данных из TradingView;

if (Требуется дополнительная информация?) then (Да)
  :Получение данных из CoinGecko;
else (Нет)
endif

:Сопоставление данных вручную;
:Ручной анализ данных;

if (Информации достаточно?) then (Да)
  :Принятие решения пользователем;
  stop
else (Нет)
  :Повторный поиск данных в дополнительных сервисах;
  :Получение данных из CoinMarketCap;
  :Получение данных из TradingView;
  if (Требуется дополнительная информация?) then (Да)
    :Получение данных из CoinGecko;
  else (Нет)
  endif
  :Сопоставление данных вручную;
  :Ручной анализ данных;
  :Принятие решения пользователем;
  stop
endif

@enduml