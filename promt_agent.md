# promt_agent

Краткий рабочий дневник агента по репозиторию ModelLine.

## Правила ведения

- Читать этот файл перед началом любой работы после базовых workflow-документов.
- После завершения работы добавлять короткую запись: что выяснили, что сделали, что осталось.
- Писать кратко и по делу, без длинной исторической ленты.

## Текущий контекст

### 2026-06-01

- `microservice_data` (`Markets/CcxtMarketDataClient.cs` + factory/DI/Settings/csproj/appsettings, README/STRUCTURE): dataset OHLCV/funding/OI ingest переведён на унифицированный **ccxt** (официальный NuGet `ccxt`). Провайдер выбирается флагом `DataService:Dataset:OhlcvProvider` (`ccxt` default \| `native` откат к рукописным адаптерам). Новая биржа = запись в `DataService:Dataset:CcxtExchanges`, без нового REST-адаптера. Свап изолирован за `IMarketDataClient`/`MarketDataClientFactory`, остальной pipeline не тронут. Caveat: `turnover ≈ close×volume` (в unified OHLCV нет quote-volume), значения `double`; live-WS остался на нативных SDK. На backend-host: `dotnet build … -c Release` зелёный, контейнер `microservice_data` пересобран/перезапущен, лог фабрики показывает `OhlcvProvider=ccxt`, e2e через gateway chart подтверждает ccxt-ингест (`turnover==close*volume` в `btcusdt_5m`, реальные цены, без ошибок). Осталось (опционально): мигрировать live-WS на ccxt.pro `WatchOHLCV`/`WatchBookTicker` и/или вернуть kraken — через ccxt это теперь добавление id в конфиг.

### 2026-05-24

- `microservice_gateway/src/GatewayService.API/{Market/IDataServiceClient.cs,Market/DataServiceClient.cs,Market/ChartService.cs,Market/IChartService.cs}`, `microservice_gateway/tests/{GatewayService.UnitTests/Market/ChartServiceTests.cs,GatewayService.UnitTests/Market/DataServiceClientTests.cs,GatewayService.UnitTests/MarketControllerTests.cs,GatewayService.IntegrationTests/MarketQueueIntegrationTests.cs}`: закрыт следующий chart regression из фронтенд-ТЗ. Root cause был уже не только в false `503`, а в том, что gateway на cold miss / partial window держал background-only hydrate semantics и слишком рано отдавал `pending`. Chart path теперь при свободном ingest-lock делает bounded synchronous queued ingest через `dataset_jobs`, перечитывает rows в том же HTTP-запросе и делает ещё один bounded reread сразу после ingest completion, чтобы first hit чаще возвращал `ok`/`partial` вместо пустого `pending`. Узкая проверка зелёная: focused unit tests по `ChartService`/`DataServiceClient`/`MarketController` и `MarketQueueIntegrationTests`. Важное внешнее ограничение: Flutter repo по присланному пути `/home/ubuntu/VKR_2026_crypt` в этой среде не найден, поэтому frontend patch здесь не применялся.

- `microservice_account/src/AccountService.Application/DTOs/Requests/LoginRequest.cs`, `microservice_account/tests/AccountService.ContractTests/AccountApiContractTests.cs`: закрыт login defect с binder error `Deserialization of types without a parameterless constructor... LoginRequest`. Причина была в том, что у positional record появился второй convenience constructor, и `System.Text.Json` перестал понимать, какой ctor использовать для `[FromBody]`. Primary ctor помечен `JsonConstructor`; contract test теперь отдельно шлёт `{ login, password }` и доходит до нормального `401`, а не до deserialization exception. Узкая проверка: build API, login unit tests и targeted contract test зелёные.

- `microservice_gateway/src/GatewayService.API/{Market/IDataServiceClient.cs,Market/DataServiceClient.cs,Market/ChartService.cs,Controllers/MarketController.cs,Market/IChartService.cs}`, `microservice_gateway/tests/GatewayService.UnitTests/{Market/ChartServiceTests.cs,MarketControllerTests.cs}`: закрыт defect с ложным `pending` на public `GET /api/v1/market/chart`. Gateway теперь разделяет empty rows, `claim_check` и explicit downstream rows failures/timeouts; `pending` остаётся только для реального background hydrate/claim-check, а `DATA_SOURCE_UNAVAILABLE` / `DOWNSTREAM_TIMEOUT` / `SERVICE_BUSY` уходят в `503`. Узкая проверка зелёная: 4 targeted unit tests по `ChartService` + `MarketController`.

- `microservice_data/src/DataService.API/{Jobs/IDatasetJobHandler.cs,Jobs/JobHandlers.cs}`, `microservice_admin/src/{hooks/useDatasetJobs.ts,components/DatasetJobsPanel.tsx}`: закрыт defect с зависшими dataset jobs на `compute_features`, где операторский `Отменить` выглядел сломанным. Причина была локальная: `jobs.cancel` ставил только `cancel_requested`, но длинные DB stages `upsert` / `compute_features` не работали под cancelable token. `JobContext` теперь poll-ит cancel flag и рвёт linked token для монолитных SQL вызовов, а admin local store сразу помечает accepted cancel как `Отмена...` до terminal-state. Узкая проверка: `dotnet build src/DataService.API/DataService.API.csproj`, diagnostics по изменённым admin/data файлам чистые.

- `microservice_gateway/src/GatewayService.API/{Clients/Market/MarketServiceClient.cs,Clients/Market/IMarketServiceClient.cs,Controllers/MarketController.cs,DTOs/Responses/MarketSnapshotResponses.cs,Market/DataTopics.cs}`, `microservice_gateway/tests/GatewayService.IntegrationTests/{GatewayTestWebAppFactory.cs,MarketIntegrationTests.cs}`: в public market API добавлен watcher-backed realtime quote route `GET /api/v1/market/quotes/realtime`. Gateway читает live rows через `cmd.data.market_watcher.rows`, мержит их с snapshot 24h stats и при отсутствии live row деградирует в `snapshot-fallback`, а не роняет фронтенд. Focused validation зелёная: `dotnet test tests/GatewayService.IntegrationTests/GatewayService.IntegrationTests.csproj --filter "FullyQualifiedName~MarketIntegrationTests"` -> 36/36, `dotnet build src/GatewayService.API/GatewayService.API.csproj` -> success.

- `microservice_gateway/src/GatewayService.API/{Market/MarketCacheService.cs,Market/ChartService.cs,Market/ChartRequestQueue.cs,Controllers/MarketController.cs,Market/MarketSettings.cs,Extensions/ServiceCollectionExtensions.cs}`, `microservice_gateway/tests/{GatewayService.UnitTests/Market/ChartServiceTests.cs,GatewayService.IntegrationTests/{GatewayTestWebAppFactory.cs,MarketIntegrationTests.cs}}`: chart-path усилен под bursty read-load. В market cache добавлен короткий per-instance memory hot cache поверх distributed cache, `ChartService` научился reuse-ить bigger cached window для smaller `limit`, а `GET /api/v1/market/chart` теперь выставляет weak `ETag`, `Last-Modified` и status-aware `Cache-Control`, так что повторный polling может получить `304 Not Modified` без повторной передачи candles. Focused validation зелёная: `dotnet test tests/GatewayService.UnitTests/GatewayService.UnitTests.csproj --filter "FullyQualifiedName~ChartServiceTests|FullyQualifiedName~ChartRequestQueueTests"` -> 18/18, `dotnet test tests/GatewayService.IntegrationTests/GatewayService.IntegrationTests.csproj --filter "FullyQualifiedName~MarketIntegrationTests"` -> 34/34.

- `microservice_gateway/src/GatewayService.API/{Controllers/MarketController.cs,Controllers/NewsController.cs,Clients/Market/MarketServiceClient.cs,DTOs/Responses/{MarketSnapshotResponses.cs,FrontendContractsResponses.cs}}`, `microservice_gateway/tests/GatewayService.IntegrationTests/{MarketIntegrationTests.cs,GatewayTestWebAppFactory.cs}`: новый frontend request сдвинул market/news contract ещё на один шаг. Gateway теперь публикует dedicated `GET /api/v1/market/trending` и `GET /api/v1/market/top-movers`, понимает `collection=market|trending|top-movers` у `GET /api/v1/market/tickers`, возвращает `snapshotId` как cheap polling marker, добавляет frontend-compatible `GET /api/v1/market/convert?from=&to=&amount=` рядом с legacy `converter/quote`, а `GET /api/news` / `GET /api/news/home` принимают optional `tag` и configurable `limit`. Заодно `market/overview` перестал подставлять transport-stable нули в деградированном snapshot path и может отдавать `null` + `meta.degradedFields`. Узкая проверка зелёная: `dotnet test tests/GatewayService.IntegrationTests/GatewayService.IntegrationTests.csproj --filter MarketIntegrationTests` -> 32/32.

- `microservice_gateway/API.md`: дочищена синхронизация live HTTP-contract после предыдущего gateway change set. Добиты точные `Cache-Control` semantics для public market/news routes, уточнены auth error codes (`account_proxy_error` vs `account_profile_unavailable`), и исправлен converter `source` в примере на фактический production value `bybit-linear-tickers`. После этого нужен обычный `build -> commit -> push -> restart.sh all noadmin` цикл.

- `microservice_gateway/src/GatewayService.API/{Controllers/AccountController.cs,Frontend/FrontendContractState.cs}`, `microservice_gateway/tests/GatewayService.UnitTests/{AccountControllerTests.cs,FrontendContractStateTests.cs}`: дожат финальный gateway slice для `microservice_gateway/API_requests.md`. После первого public market/news пакета оставались два явных долга: unified auth error envelope и process-local fallback state. `AccountController` теперь нормализует gateway-managed auth/profile failures в `ErrorResponse`, а `FrontendContractState` хранит linked exchanges / alerts / toggles через `IDistributedCache`, поэтому при настроенном Redis state переживает restart и становится shared между инстансами. Узкие проверки зелёные: `dotnet build src/GatewayService.API/GatewayService.API.csproj` и `dotnet test tests/GatewayService.UnitTests/GatewayService.UnitTests.csproj --filter "AccountControllerTests|FrontendContractStateTests"`.

- `microservice_data/src/DataService.API/{Jobs/MarketWatcherService.cs,Database/DatasetRepository.cs}`, `microservice_data/{README.md,STRUCTURE.md,EXCHANGE_APIS.md}`: после следующей проверки Inspect выяснилось, что raw watcher-save уже починен, но derived fields (`return_1` и ниже) всё ещё не создавались. Причина локальная: watcher не запускал feature recompute после `BulkUpsertAsync`. В `DatasetRepository` добавлен `ComputeAndUpdateFeaturesSinceAsync` — tail-only recompute с lookback по max feature horizon, а `MarketWatcherService` теперь вызывает его сразу после записи новых raw rows. Это заполняет `return_*`, rolling stats, `atr_*` и соседние derived columns для новых watcher-свечей без full-table `compute_features` на каждый rollover. `dotnet build src/DataService.API/DataService.API.csproj` проходит.

### 2026-05-23

- `microservice_gateway/src/GatewayService.API/Kafka/KafkaRequestClient.cs`, `microservice_infra/docker-compose.yml`, `microservice_gateway/{README.md,STRUCTURE.md}`, `microservice_infra/{README.md,STRUCTURE.md}`: закрыт live split-admin incident `Kafka error: HTTP 503`. Root cause оказался в janitor policy: живой, но idle `reply.gateway.*` topic оставался с `HW=0`, `redpanda-janitor` удалял его, gateway получал `partitions revoked` и зависал в `kafka-request-reply=Unhealthy` до рестарта. Фикс: gateway теперь после успешного `CreateTopicsAsync` тоже пишет bootstrap-marker в inbox, чтобы `HW > 0` до первого реального reply. После rebuild/redeploy `http://localhost:7520/health/ready` и `https://95.165.27.159:8443/health/ready` снова `200`, новый inbox assigned, `rpk topic describe` показывает ненулевой HighWatermark.

- `microservice_data/src/DataService.API/{Jobs/MarketWatcherService.cs,Database/DatasetRepository.cs}`, `microservice_analitic/backend/{data_client.py,anomaly/session.py,anomaly/isolation_forest.py}`, `microservice_admin/src/app/anomaly/page.tsx`, `microservice_analitic/tests/test_anomaly_session_projection.py`: закрыт свежий incident по stale market watcher + slow anomaly. Root cause data-side был в том, что watcher после live-overlay refactor обновлял только `market_watch_live`, но не canonical dataset tables; fix — на rollover watcher теперь зеркалит closed O/H/L/C в dataset table через `BulkUpsertLiveCandlesAsync`, не трогая non-live колонки. После rebuild `microservice_data` проверка в Postgres показала `btcusdt_1m max timestamp` уже `2026-05-23 04:47:00` при текущем времени `04:48`, то есть canonical table снова почти realtime. Analytics-side bug был в mismatch `timestamp_ms` vs `timestamp_utc`: DBSCAN/IForest падали на реальном parquet. `session.py` теперь нормализует timestamp в `timestamp_ms`, держит маленький parquet read-cache и сбрасывает его при session switch; `data_client.py` и `isolation_forest.py` переведены на этот contract. Runtime validation после rebuild `microservice_analitic`: `dbscan_error=None`, `iforest_error=None`, повторные bounded reads падают примерно `0.309s -> 0.008s`, а в контейнере handlers больше не ловят `KeyError`. Admin `/anomaly` ускорен ещё и с UI-стороны: `Analyze` сначала берёт быстрый `count_only` profile и догружает полный column stats в фоне.

- `microservice_gateway/src/GatewayService.API/{Controllers/AccountController.cs,Controllers/MarketController.cs,Controllers/PortfolioController.cs,Controllers/ExchangesController.cs,Controllers/AlertsController.cs,Controllers/ServiceTogglesController.cs,Controllers/MobileAdminController.cs,Clients/Account/*,Clients/{Market,News,Notifications,Portfolio}/*,DTOs/Requests/FrontendContractsRequests.cs,DTOs/Responses/FrontendContractsResponses.cs,Frontend/*,Extensions/ServiceCollectionExtensions.cs}`, `microservice_account/src/AccountService.Application/{DTOs/Requests/LoginRequest.cs,DTOs/Responses/AuthResponse.cs,Services/AccountAppService.cs,Validators/LoginRequestValidator.cs}`, `microservice_account/tests/AccountService.UnitTests/Services/AccountAppServiceTests.cs`: закрыт frontend-contract slice из `microservice_gateway/API_requests.md`. Gateway теперь публикует auth passthrough `POST /api/account/{register,login,refresh,logout}`, public `GET /api/v1/market/overview`, personal `GET /api/portfolio/summary`, `/api/exchanges/*`, `/api/alerts/*`, `/api/services/toggles` и lightweight mobile-admin `GET /api/admin/{summary,users,services,statistics}`. Большая часть новых frontend payload-ов пока строится из gateway-local in-memory state/fallbacks и сбрасывается при restart. В `microservice_account` login принимает `email` или `login`, а auth response получил top-level `id` и `email` рядом с `uid`. Для завершения этого slice Markdown синхронизирован, остаются финальные build/push.

- `microservice_data/src/DataService.API/{Settings/DataServiceSettings.cs,Jobs/MarketWatcherService.cs,appsettings.json}`, `microservice_data/{README.md,STRUCTURE.md,EXCHANGE_APIS.md}`: новый operator-request был проще предыдущего safe-degraded path: Kraken для live сейчас не нужен, его нужно убрать из default runtime совсем. Root cause оказался локальным: default `DataService:MarketWatch:Exchanges` и fallback `NormalizeExchanges(...)` всё ещё возвращали `kraken`, из-за чего watcher после каждого рестарта честно стартовал в `running + degraded` и показывал `429 RateLimitRequest` в UI. Фикс: default/fallback live exchanges теперь только `bybit` + `binance`; Kraken оставлен как manual opt-in path. После rebuild/redeploy проверка через gateway `/api/admin/market-watcher/{status,rows}` показала `status=running`, `message="Watching 34 symbols across binance, bybit"`, `exchanges=["bybit","binance"]`, `liveRows=34`, `lastError=null`, rows только по `binance` и `bybit`.

### 2026-05-22

- `microservice_data/src/DataService.API/{Jobs/MarketWatcherService.cs,Jobs/MarketWatcherRuntimeState.cs}`, `microservice_data/{README.md,STRUCTURE.md,EXCHANGE_APIS.md}`: после уже достигнутого `3 exchanges ~1s` всплыл следующий operational defect: occasional Kraken websocket startup `429` (`RateLimitRequest`) мог снести весь watcher even though Binance и Bybit уже тикали. Попытка сделать restart-based self-heal оказалась небезопасной для этого окружения, поэтому финальный safe fix другой: Kraken startup rate-limit после живых healthy venues теперь переводит watcher в `running` + degraded message, сохраняет Binance/Bybit online и сразу чистит stale Kraken rows из runtime state и `market_watch_live`. Последняя проверка через gateway `/api/admin/market-watcher/{status,rows}` после rebuild/redeploy: `trackedSymbols=34`, `liveRows=34`, без stale Kraken rows, fallback lag примерно `binance ~361 ms`, `bybit ~752 ms`.

- `microservice_data/src/DataService.API/{Jobs/MarketWatcherService.cs,Markets/KrakenApiClient.cs}`, `microservice_data/{README.md,STRUCTURE.md,EXCHANGE_APIS.md}`: дожат финальный runtime-target для live watcher-а. После нескольких runtime итераций выяснилось, что проблема уже не только в Kraken discovery, а в том, что sparse best-offer changes оставляли тихие Kraken pairs stale на 2-9 секунд. Watcher получил per-exchange startup retries с all-or-nothing semantics, Kraken переведён на managed `KrakenSpotSymbolOrderBook`, а synced top-of-book теперь дополнительно переиздаётся коротким refresh-loop'ом. Финальный срез через gateway `/api/admin/market-watcher/{status,rows}` после redeploy: `binance avg ~124 ms`, `bybit avg ~323 ms`, `kraken avg ~251 ms`, общий `max ~919 ms`, то есть условие «3 биржи и lag около 1s или меньше» выполнено.

- `microservice_data/src/DataService.API/{Jobs/MarketWatcherService.cs,Markets/KrakenApiClient.cs}`, `microservice_data/{README.md,STRUCTURE.md,EXCHANGE_APIS.md}`: после backend-only рестарта выяснилось, что `kraken` пропадает из live watcher-а по двум связанным причинам. Сначала startup discovery упирался в полный `Kraken /AssetPairs` catalog fetch и выбрасывал всю биржу по deadline; после перевода на narrow per-pair resolve проявился второй локальный дефект: Kraken metadata для `BTCUSDT` возвращала `XBTUSDT/XBT/USDT`, а websocket subscription такой alias не принимал. Фикс: watcher теперь адресно резолвит только whitelist `BASE/USDT` pairs, unsupported symbols quietly skip-ает и подписывается по canonical `BASE/USDT` alias. После redeploy `market_watch_live` снова содержит `kraken` rows.

- `microservice_data/src/DataService.API/{Database/DatasetJobsMutator.cs,Jobs/DatasetJobRunner.cs}`, `microservice_data/{README.md,STRUCTURE.md}`: во время live-проверки всплыл последний queue хвост после migration на dedicated MarketWatcherService. Сам watcher уже не использовал `dataset_jobs`, но `cmd.data.dataset.jobs.list` всё ещё возвращал historical failed `market_watch` rows из базы. Startup recovery теперь после reclaim invalid/orphan jobs физически удаляет все legacy `type='market_watch'`, чтобы queue API и operator Queue видели только реальные dataset jobs.

- `microservice_data/src/DataService.API/Jobs/MarketWatcherService.cs`, `microservice_data/{README.md,STRUCTURE.md,EXCHANGE_APIS.md}`: добит remaining live-lag defect после уже исправленной lag-математики. Root cause подтвердился на runtime: sparse ticker/last-trade feeds, особенно у Bybit и части thin Binance symbols, держали watcher stale despite correct owner-side aggregates. Watcher переведён на websocket book-side liveness: Binance теперь использует targeted USD-M book-ticker subscriptions по whitelist symbols, Bybit — linear depth-1 orderbook; в обоих случаях live price считается как midpoint best bid/ask. Узкий `dotnet build src/DataService.API/DataService.API.csproj` проходит, после rebuild `microservice_data` protected `/admin/api/kafka` показал `avgLagMs=400`, `maxLagMs=3552` при baseline около `5438/51006`, то есть цель по average lag `<1s` достигнута без возврата к REST polling или лишним DB writes.

- `microservice_data/src/DataService.API/{Jobs/MarketWatcherService.cs,Database/MarketWatchRepository.cs,Settings/DataServiceSettings.cs,Dataset/DatasetConstants.cs,appsettings.json}`, `microservice_data/{README.md,STRUCTURE.md}`: live Market Watcher universe ограничен dataset symbol whitelist-ом. Выяснилось, что `Dataset Configuration` в admin — это не отдельная DB-конфигурация, а продуктовый список поддерживаемых symbols; data-service теперь фильтрует raw exchange discovery по `DataService:MarketWatch:Symbols`, а дефолт синхронизирован с тем же набором (`BTCUSDT` … `APTUSDT`). Во время runtime-проверки всплыл соседний startup defect: Kraken discovery мог подвесить watcher в `starting`, если упирался в shared limiter; `MarketWatcherService` теперь режет discovery каждого exchange отдельным дедлайном и уходит в `degraded`/fallback вместо бесконечного старта. Дополнительно startup prune-ит `market_watch_live` до текущего tracked universe, а broad feeds вроде Binance all-ticker теперь режутся по whitelist ещё на callback-входе, так что `liveRows` больше не раздувается чужими symbols. Проверено: `dotnet build src/DataService.API/DataService.API.csproj` проходит; после рестарта `/admin/market-watcher` должен выйти в `running` c whitelist-only rows.

- `microservice_admin/src/lib/backendClient.ts`, `microservice_gateway/src/GatewayService.API/{Controllers/AdminController.cs,Kafka/AdminTopics.cs}`, `microservice_admin/STRUCTURE.md`, `microservice_gateway/{README.md,API.md,STRUCTURE.md}`: закрыт split-mode defect страницы `/market-watcher`. Root cause был не в data-service watcher-е, а в transport-layer: после добавления новых Kafka topics admin backend facade не получил ни `TOPIC_PATH` mapping, ни HTTP routes в gateway. Добавлены `POST /api/admin/market-watcher/{status,set-enabled,rows,logs}` и соответствующие topic-константы, поэтому Market Watcher теперь работает и через backend-host split deployment, а не только в local direct-Kafka mode.

- `microservice_data/src/DataService.API/{Jobs/MarketWatchBootstrapService.cs,Jobs/MarketWatchJobHandler.cs,Database/MarketWatchRepository.cs,Database/DatasetJobModels.cs,Jobs/DatasetJobRunner.cs,Program.cs,Settings/DataServiceSettings.cs,Markets/IMarketDataClient.cs,Bybit/BybitApiClient.cs,Markets/BinanceApiClient.cs,Markets/KrakenApiClient.cs,DataService.API.csproj}`, `microservice_data/{README.md,STRUCTURE.md,EXCHANGE_APIS.md}`: добавлен perpetual `market_watch` worker. Он автоматически держит один queue-visible singleton job `type=market_watch`, получает realtime prices по websocket (`Binance.Net`, `Bybit.Net`, `KrakenExchange.Net`), локально агрегирует current candles по всем настроенным TF и пишет live overlay в отдельную таблицу `market_watch_live`, не конфликтуя с историческими dataset tables. Текущий Kraken live scope сознательно ограничен `*USDT` spot universe, а authoritative historical candles по-прежнему принадлежат ingest/repair/upsert path.

- `microservice_data/src/DataService.API/{Jobs/MarketWatcherService.cs,Jobs/MarketWatcherRuntimeState.cs,Database/MarketWatchRepository.cs,Kafka/KafkaConsumerService.cs,Kafka/Topics.cs,Program.cs,Jobs/JobHandlers.cs,Jobs/DatasetJobRunner.cs,Database/DatasetJobsMutator.cs}`, `microservice_admin/src/{app/market-watcher/page.tsx,components/Sidebar.tsx,lib/topics.ts,lib/i18n.ts,hooks/useDatasetJobs.ts,app/queue/page.tsx,lib/types.ts}`, `microservice_data/{README.md,STRUCTURE.md}`, `microservice_admin/{README.md,STRUCTURE.md}`: queue-visible `market_watch` переделан в отдельный hosted runtime market watcher внутри `microservice_data`. Он больше не живёт в `dataset_jobs`, управляется через dedicated Kafka topics `cmd.data.market_watcher.{status,set_enabled,rows,logs}`, пишет watcher-only runtime logs и realtime rows с `lag_ms`, а admin получил отдельную страницу `/market-watcher` как 8-й пункт меню. Queue/Queue History больше не включают legacy/new watcher traffic; `Duration` для terminal dataset jobs теперь считается как `started_at → finished_at`. Параллельно ingest-path получил cheap coverage short-circuit перед `FindMissingTimestampsAsync`, чтобы high-TF no-op окна не тратили время на дорогой missing-scan.

- `microservice_data/src/DataService.API/{Bybit/BybitApiClient.cs,Jobs/JobHandlers.cs,Dataset/DatasetConstants.cs}`, `microservice_data/{README.md,STRUCTURE.md,EXCHANGE_APIS.md}`: проведён тот же real benchmark-cycle для Bybit. Сначала всплыл локальный parse defect в `FetchInstrumentDetailsAsync`: `launchTime` у `/v5/market/instruments-info` приходит строкой, поэтому client переведён на tolerant parse для string/number. Затем подтверждено, что bottleneck у Bybit был не в merge-path, а в слишком консервативном heavy kline fan-out: реальный benchmark на 1,000,000 свечей `BTCUSDT` 1m сократился примерно с `2:17` до `0:51`, когда heavy `maxParallel` подняли с `2` до `6`. Production policy обновлена соответственно. Kraken попытались прогнать тем же циклом, но из текущего окружения это пока не воспроизводится стабильно: даже прямой host `curl` на reachable `OHLC` window (`interval=60`) таймаутится за `15 s` с частично полученным body, поэтому Kraken speed-tuning в этом срезе не публиковался.

- `microservice_data/src/DataService.API/{Jobs/JobHandlers.cs,Dataset/DatasetConstants.cs,Markets/BinanceApiClient.cs,Markets/BinanceRateLimiter.cs}`, `microservice_data/{README.md,STRUCTURE.md,EXCHANGE_APIS.md}`: ускорен Binance historical fetch path без изменения scheduler heavy-serialization. Heavy `1m`/`3m` Binance jobs по-прежнему идут по одному на биржу, но внутри job fan-out поднят до `6`, `BinanceRateLimiter` ослаблен до `6` inflight / `35 ms` per unit, а `BinanceApiClient` перестал собирать million-candle pages через полный dictionary+sort pass. Временный реальный benchmark на 1,000,000 свечей (`BTCUSDT`, 1m) показал улучшение fetch stage примерно с `11:14` до `3:56` (`~2.85x`).

- `microservice_data/src/DataService.API/Markets/{BinanceApiClient.cs,KrakenApiClient.cs}`, `microservice_data/{README.md,STRUCTURE.md,EXCHANGE_APIS.md}`: разобран новый live binance/kraken ingest slice из queue logs. Root cause binance fail-ов `HTTP 400 parameter 'startTime' is invalid` оказался в слишком широком local retention clamp для `/futures/data/openInterestHist`: клиент держал 31 день, тогда как upstream практически режет историю строгими 30 днями. Clamp переведён на `30d` + существующее выравнивание по границе периода. Для Kraken добавлен safe fast-path: если клипованное окно помещается в reachable 720-candle history, client сначала пробует один `GET /0/public/OHLC` на весь диапазон и только при неполном покрытии откатывается к текущему throttled paged fetch. Проверено: `dotnet build src/DataService.API/DataService.API.csproj` проходит; прямой `curl` на старый Binance URL даёт `400`, а на тот же запрос после сдвига `startTime` внутрь 30-дневного окна — `200`.

- `microservice_admin/src/app/download/page.tsx`, `microservice_admin/{README.md,STRUCTURE.md}`: добавлен `Exchange=ALL` для Dataset Download. Aggregate ingest теперь fan-out'ит не только по `symbol × timeframe`, но и по выбранным биржам, а tracked jobs ключуются как `exchange::symbol::timeframe`, чтобы `binance`/`kraken` не коллидировали в одном ALL-виджете. Для aggregate режимов `Symbol=ALL`/`Exchange=ALL` single-table actions (`coverage/export/repair/delete`) оставлены отключёнными. Проверено: diagnostics чистые, `docker build -t microservice-admin-exchange-all-check .` проходит.

- `microservice_admin/src/{app/download/page.tsx,lib/constants.ts}`, `microservice_admin/{README.md,STRUCTURE.md}`: добавлен режим `Symbol=ALL` для Dataset Download. Admin теперь fan-out'ит ingest по всем датасетам выбранной биржи и всем TF через обычные `cmd.data.dataset.jobs.start`, а `AllIngestProgress` отслеживает их по ключу `symbol::timeframe`. Для `Symbol=ALL` coverage/export/repair/delete сознательно отключены, потому что эти операции всё ещё адресуют одну таблицу. Проверено: diagnostics по изменённым TS-файлам чистые, `docker build -t microservice-admin-all-datasets-check .` проходит.

- `microservice_data/src/DataService.API/{Database/DatasetJobModels.cs,Database/DatasetJobsRepository.cs,Database/DatasetJobsMutator.cs,Jobs/IDatasetJobHandler.cs,Jobs/JobHandlers.cs,Jobs/DatasetJobRunner.cs,Kafka/KafkaConsumerService.Jobs.cs,Markets/BinanceApiClient.cs,Markets/BinanceRateLimiter.cs,Program.cs}`, `microservice_admin/src/{lib/types.ts,hooks/useDatasetJobs.ts,components/DatasetJobsPanel.tsx}`, `microservice_data/{README.md,STRUCTURE.md}`, `microservice_admin/{README.md,STRUCTURE.md}`: переведён live progress dataset jobs на честную dual-model схему. Backend теперь хранит и публикует не только weighted overall `progress`, но и current-stage snapshot (`stage_progress`, `stage_total`, `stage_completed`, `stage_failed`, `stage_skipped`), а admin Queue показывает отдельные stage/overall бары вместо одного смешанного процента. Заодно найден и закрыт текущий binance slowdown: причиной были не scheduler slots, а upstream `418` IP ban на kline path и `400 parameter 'startTime' is invalid` на `openInterestHist`. В Binance client добавлен process-local limiter с `Retry-After` penalty, а open interest теперь заранее клипуется к latest-month retention и выравнивается по границе интервала. Проверено: `dotnet build DataService.sln` и `docker compose build admin admin-online` проходят.

- `microservice_admin/{Dockerfile,.dockerignore,README.md,STRUCTURE.md}`: добавлен ещё один слой кэширования для Node/Next build path admin-сервиса. В Dockerfile появились именованные cache mounts для npm и `.next/cache`, `next build` идёт с `NODE_COMPILE_CACHE`, а `.dockerignore` перестал включать в context docs/proxy файлы, не влияющие на standalone image. Проверено: cold `docker build -t microservice-admin-cache-check .` проходит, повторный build подхватывает cached `npm ci` и cached `next build`.

- `microservice_admin/src/{app/page.tsx,lib/i18n.ts}`, `microservice_admin/{README.md,STRUCTURE.md}`: dashboard dataset-summary приведён к multi-exchange виду. Добавлен exchange dropdown по реально доступным биржам, coverage-chart сделан выше и теперь строится только по непустым таблицам выбранной биржи, а `Tables`/`Total Tables` больше не учитывают `0 rows`. Проверено через `docker compose build admin` и `docker compose build admin-online`.

- `microservice_data/src/DataService.API/{Markets/KrakenApiClient.cs,Markets/KrakenRateLimiter.cs,Program.cs}`, `microservice_data/{README.md,STRUCTURE.md,EXCHANGE_APIS.md}`: исправлен новый live-slice для Kraken после 4-slot ingest pool. Причина была в том, что scheduler честно запускал до 4 Kraken jobs, а сам Kraken public REST path не имел своего локального throttle и быстро отвечал `EGeneral:Too many requests`. Внутрь Kraken client добавлен process-local limiter + retry/backoff на Kraken throttle ошибки. После rebuild и живого Kafka validation (`cmd.data.dataset.jobs.start` для `120m/240m/720m/1d`) `120m` и `240m` уже ушли в `succeeded`, а `720m` и `1d` остались живыми `running` с heartbeat вместо мгновенного fail.

- `microservice_data/src/DataService.API/{Jobs/JobHandlers.cs,Markets/IMarketDataClient.cs,Markets/BinanceApiClient.cs,Bybit/BybitApiClient.cs,Markets/KrakenApiClient.cs}`, `microservice_data/{README.md,STRUCTURE.md}`: проверен текущий incident со «стоящими» jobs на `fetch_oi`. В Postgres job-ы были живы (`updated_at` age ~2–3 s), то есть runner не завис; root cause был в прогрессе: `fetch_oi` всегда держал фиксированный `47%`, потому что только `fetch_klines` умел page callbacks. В market-client interface добавлен `onPageDone` для open interest, Bybit/Binance теперь публикуют completed OI pages, а ingest-job progress начал показывать реальную степень завершённости `fetch_oi`.

- `microservice_data/src/DataService.API/{Jobs/JobHandlers.cs,Markets/KrakenApiClient.cs}`, `microservice_admin/src/{hooks/useDatasetJobs.ts,app/download/page.tsx,app/api/queue/history/route.ts,components/DatasetJobsPanel.tsx}`, `microservice_data/EXCHANGE_APIS.md`: докручен следующий слой после per-exchange queue fix. Kraken symbol resolution больше не ходит в полный `AssetPairs` catalog — используется узкий `AssetPairs?pair=...`, а полностью недостижимые historical windows завершаются как no-op. В admin failed/canceled dataset jobs теперь автоскрываются через 10 с, terminal completions пишутся в queue history, а Download дополнительно сохраняет ingest errors в local Action History, чтобы экран не расползался от накопленных ошибок. Проверено: `dotnet build src/DataService.API/DataService.API.csproj` проходит, TS diagnostics по изменённым admin-файлам чистые.

- `microservice_data/src/DataService.API/{Jobs/DatasetJobRunner.cs,Jobs/JobHandlers.cs,Kafka/KafkaProducer.cs,Markets/KrakenApiClient.cs,Markets/BinanceApiClient.cs,Bybit/BybitApiClient.cs}`, `microservice_data/{README.md,STRUCTURE.md}`: разобран stuck-ingest incident в data-service. Выяснилось, что 4 глобальных ingest slot-а полностью заняли старые Kraken jobs, из-за чего Bybit оставался в `queued`, а progress publish мог клинить handler на Kafka delivery path. Runner переведён на per-exchange ingest pool `4` + heavy gate `1` для каждой биржи, progress events стали best-effort без блокировки job handler, Kraken ingest теперь клипует окно к реально доступной lookback-history (последние 720 base candles), а exchange HTTP clients читают response body целиком под общим `RequestTimeoutSeconds`, чтобы body-read не зависал вне timeout. Проверено: `dotnet build src/DataService.API/DataService.API.csproj` проходит, `microservice_data` rebuilt healthy, requeued `bybit` `720m` job отрабатывает сразу параллельно с живой `kraken` job, то есть биржи больше не делят один общий ingest-slot pool.

- `microservice_account/src/AccountService.API/{Extensions/ServiceCollectionExtensions.cs,Extensions/MigrationExtensions.cs}`, `microservice_account/{README.md,STRUCTURE.md}`, `docs/agents/services/microservice_account.md`: разобран реальный источник admin login error `fetch failed`. Падала не форма `microservice_admin`, а `account_service_api`: runtime видел пустую application schema с одной `__EFMigrationsHistory`, bootstrap-admin падал на `relation "roles" does not exist`, из-за чего `/api/auth/login` не мог достучаться до Account Service. Startup migration теперь логирует discovered/pending migrations, при полностью пустой schema восстанавливает core tables из текущей EF-модели и продолжает bootstrap-admin seed. После rebuild `account_service_api` снова healthy, `POST http://localhost:7510/api/account/login` и `POST http://localhost:8501/admin/api/auth/login` отвечают `200` для `admin/admin`.
- `microservicestarter/{start.sh,restart.sh}`, `microservicestarter/{README.md,STRUCTURE.md}`: bash onlyadmin-launcher теперь после вопроса про backend host/base URL сам синхронизирует и direct runtime env для admin (`ACCOUNT_URL`, `GATEWAY_URL`, `REDPANDA_ADMIN_URL`, `MINIO_URL`, `KAFKA_BOOTSTRAP_SERVERS`, `BACKEND_CONNECTION_TARGET`), а также печатает полный env summary в консоль. Это убирает ручную правку `microservice_admin/.env` для сценариев вроде `npm run dev/start` вне Docker.
- `microservice_account/src/AccountService.API/Extensions/MigrationExtensions.cs`, `tests/AccountService.UnitTests/Extensions/MigrationExtensionsTests.cs`, `microservice_account/{README.md,STRUCTURE.md}`: bootstrap admin на старте контейнера теперь ищется по username, а не только по email. Если nickname `admin` отсутствует, startup migration создаёт bootstrap-user или дочиняет существующего bootstrap-user по email, присваивая ему ожидаемый username и admin role. Добавлены focused unit tests на оба сценария.
- `microservice_account/src/AccountService.API/Extensions/MigrationExtensions.cs`, `src/AccountService.Application/*`, `microservice_account/{.env.example,README.md,STRUCTURE.md}`, `microservice_admin/{README.md,STRUCTURE.md}`: добавлен first-run bootstrap admin `admin/admin` для пустой users table без явного `AdminBootstrap:*`. Login в account-service стал email-or-username, поэтому admin-панель теперь может входить по логину `admin`; custom bootstrap env по-прежнему имеет приоритет. `dotnet build AccountService.sln` и `npm run build` в admin проходят.
- `microservice_admin/src/middleware.ts`, `microservice_admin/{README.md,STRUCTURE.md}`, `docs/agents/services/microservice_admin.md`: доделан admin login cache. Middleware теперь умеет silently refresh-ить access token через Account Service по refresh cookie до рендера защищённых pages/API и редиректит `/login` обратно в панель, если cached admin-session ещё восстановима. `npm run build` в `microservice_admin` снова проходит.

### 2026-05-21

- `microservice_account`, `microservice_gateway`, `microservice_admin`, `microservicestarter`, `deploy/*`, docs: начат крупный auth-refactor без статических admin-ключей. Account теперь знает роли `guest` / `user` / `admin`, auth responses возвращают UID/accountType/roles, startup может bootstrap/promote login-only admin. Gateway `/api/admin/*` защищён JWT с ролью `admin`, admin UI получил `/login`, httpOnly session cookies и logout, split backend calls пересылают admin JWT. Launcher больше не генерирует и не синхронизирует общий admin-ключ, token helper scripts удалены. Осталось прогнать build/test и добить возможные compile/type errors.
- `microservice_gateway/src/GatewayService.API/Kafka/{IKafkaRequestClient.cs,KafkaRequestClient.cs,KafkaRequestReplyHealthCheck.cs}`, `Program.cs`, `tests/GatewayService.UnitTests/KafkaRequestReplyHealthCheckTests.cs`, `microservice_gateway/{README.md,API.md,STRUCTURE.md}`: backend diagnostics для split admin усилены вокруг reply inbox readiness. Gateway теперь хранит `ReplyInboxStatus` (последнюю причину/стадию bootstrap → subscribe → assignment), прокидывает её в `/health/ready` JSON и в detail `admin_kafka_timeout`, а readiness wait больше не висит весь route timeout: если inbox не ready, gateway fast-fail'ится по короткому budget и раньше возвращает structured `504`, что убирает часть generic `admin_backend_timeout` на admin-host.
- `deploy/modelline-deploy.yml`, `deploy/reconcile.sh`, `README.md`, `STRUCTURE.md`, `microservice_gateway/README.md`, `microservice_gateway/STRUCTURE.md`: найден дополнительный backend deployment drift, который мешал выкатывать split-admin fixes на live. Root deploy config ссылался на несуществующие compose service names (`gateway`, `analitic`, `account`, `kafka/postgres/redis` в infra), а `reconcile.sh` падал на втором YAML entry из-за `set -e` + `(( idx++ ))`. Конфиг выровнен на реальные сервисы (`gateway-service`, `api`, `account-api`, `redpanda`, `nginx`, `minio`, `redpanda-console`, `redpanda-janitor`), infra переведена на explicit restart в reconcile, parser исправлен, dry-run теперь проходит по всему backend deployment graph.
- `deploy/{print_token.sh,set_token.sh}`, `README.md`, `STRUCTURE.md`, `microservice_gateway/README.md`, `microservice_gateway/STRUCTURE.md`: ранний split admin token workflow был упрощён перед последующим удалением в пользу admin JWT session.
- `microservice_gateway/src/GatewayService.API/Kafka/{KafkaRequestClient.cs,IKafkaRequestClient.cs,KafkaRequestReplyHealthCheck.cs}`, `Program.cs`, `Extensions/ServiceCollectionExtensions.cs`, `tests/GatewayService.UnitTests/KafkaRequestReplyHealthCheckTests.cs`: исправлен backend-side split admin incident в gateway request/reply bootstrap. Reply inbox `reply.gateway.{instanceId}` больше не считается ready до фактического consumer assignment; если Kafka Admin create не подтвердил topic в startup budget, gateway теперь bootstrap-ит topic через producer publish в сам reply inbox и продолжает retry-loop до готовности. Дополнительно `GET /health/ready` теперь требует не только Kafka bootstrap reachability, но и реальную readiness reply-inbox path, чтобы broken admin request/reply больше не выглядел healthy.

### 2026-05-19

- `microservice_gateway/src/GatewayService.API/Controllers/DashboardController.cs`, `Aggregators/Dashboard/{IDashboardAggregator.cs,DashboardAggregator.cs}`, `tests/GatewayService.UnitTests/DashboardAggregatorTests.cs`, `tests/GatewayService.IntegrationTests/GatewayIntegrationTests.cs`: guest-доступ для Flutter API выровнен с требованием продукта. `GET /api/dashboard` переведён в optional-auth: без JWT backend больше не режет главный экран `401`, а отдаёт guest payload с public sections. При этом `portfolio` вообще не запрашивается и не попадает в `degradedSections`, а personal endpoints `account/me` и `notifications` остались под JWT. Зафиксировано, что `guest` в этом API — это anonymous gateway caller, а не отдельная роль в account БД.
- `microservice_gateway/src/GatewayService.API/Settings/CorsSettings.cs`, `Extensions/ServiceCollectionExtensions.cs`, `Program.cs`, `Controllers/AdminController.cs`, `appsettings*.json`, `tests/GatewayService.IntegrationTests/GatewayIntegrationTests.cs`: найден и исправлен browser connectivity blocker gateway. Для public/protected route-ов включён CORS и корректный preflight до auth, поэтому web-клиенты больше не должны падать на `Failed to fetch` при `GET /api/news` и `OPTIONS /api/dashboard`. `/api/admin/*` специально оставлен без CORS как server-to-server facade. Есть focused integration tests на cross-origin GET и preflight.
- `microservice_gateway/API.md`: синхронизирован HTTP-контракт gateway с фактическим кодом. В документ добавлены split/local base URLs, отдельный раздел по `/api/admin/*` facade с route families и auth/503/504 semantics, плюс убрана устаревшая `empty` coverage semantics у `market/chart`.
- `microservice_data/src/DataService.API/Kafka/Topics.cs`, `src/DataService.API/Kafka/KafkaConsumerService.cs`, `microservice_analitic/backend/dataset/repair.py`, `backend/data_client.py`, `tests/test_repair.py`, `microservice_admin/src/app/download/page.tsx`: Dataset repair стал полностью exchange-aware. `cmd.analitic.dataset.load_ohlcv` больше не ходит в Bybit из Python, а делегирует `cmd.data.dataset.repair_ohlcv` владельцу данных; тот сам использует `bybit` / `binance` / `kraken`, публикует прежний `events.analitic.dataset.repair.progress`, а admin Download больше не блокирует repair для non-Bybit tables. `recompute_features` тоже резолвит правильную exchange-aware таблицу перед `compute_features`.
- `microservice_data/src/DataService.API/Dataset/DatasetCore.cs`, `src/DataService.API/Markets/*`, `src/DataService.API/Jobs/JobHandlers.cs`, `src/DataService.API/Kafka/KafkaConsumerService*.cs`, `microservice_admin/src/lib/constants.ts`, `src/app/download/page.tsx`, `src/app/api/export/csv/route.ts`: реальная multi-exchange dataset-поддержка включена для `bybit` / `binance` / `kraken`. Table naming стало exchange-aware (`btcusdt_1m` для Bybit, `binance_btcusdt_1m` / `kraken_btcusdt_1m` для остальных), ingest/jobs/export/coverage/queue теперь используют выбранную биржу end-to-end. На admin Download quality-repair сознательно заблокирован для non-Bybit tables, потому что analytic repair path всё ещё жёстко привязан к Bybit-style source selection.
- `microservice_admin/src/lib/sqliteStore.ts`, `src/app/api/cache/route.ts`, `src/lib/queueHistoryStore.ts`, `src/app/download/page.tsx`, `Dockerfile`, `docker-compose.yml`, `microservice_data/src/DataService.API/Kafka/KafkaConsumerService.Jobs.cs`, `src/DataService.API/Jobs/JobHandlers.cs`: admin state переведён с Redis/process-local persistence на SQLite (`SQLITE_DB_PATH`, named volumes `admin_sqlite_data` / `admin_online_sqlite_data`). Queue history теперь реально переживает restart контейнера, Dataset хранит выбранные `symbol/timeframe/date-range/exchange` в localStorage + server-side SQLite store, а кнопка ingest на Download получила inline exchange selector (`bybit/binance/kraken`). Backend data-service пока честно принимает только `bybit`: `jobs.start` и ingest handler возвращают `unsupported_exchange` для неподдержанных бирж.
- `microservice_admin/src/lib/queueHistoryStore.ts`, `src/app/api/queue/history/route.ts`, `src/app/api/kafka/route.ts`, `src/app/queue/page.tsx`, `src/hooks/useDatasetJobsFeed.ts`, `src/lib/i18n.ts`: Queue-экран больше не использует filtered `/api/logs` как суррогат очереди. Добавлен отдельный queue-history store с Redis-backed best-effort persistence (`modelline:queue-history:v1`), `/api/kafka` пишет туда success/error только по крупным queue-topic'ам, а `/queue` читает уже `/api/queue/history`. Таблица переименована из `API / Server Request Stream` в `Queue History`, Clear теперь чистит только queue history, а active jobs на Queue синхронизируются быстрее: poll раз в `1.5 s` плюс refresh на `focus` / `visibilitychange`.
- `microservice_admin/src/app/api/health/route.ts`, `microservice_infra/nginx/nginx.conf`, `microservice_admin/README.md`, `microservice_admin/STRUCTURE.md`, `microservice_infra/README.md`, `microservice_infra/STRUCTURE.md`: после rollout readiness-path выяснилось, что split admin health probe сломал нижние infra cards на live: admin начал ходить на `https://<backend>:8443/health/ready`, а backend facade nginx ещё публиковал только `= /health`, из-за чего dashboard показывал `HTTP 404` для redpanda/minio/account/gateway при полностью рабочих Kafka-backed health checks. Добавлен compatibility fallback `health/ready -> health` в admin route и backend nginx получил отдельный `location = /health/ready` с proxy в `gateway-service:5020/health/ready`.
- `microservice_gateway/src/GatewayService.API/Kafka/KafkaBrokerHealthCheck.cs`, `Program.cs`, `Controllers/AdminController.cs`, `DTOs/ErrorResponse.cs`, `microservice_gateway/docker-compose.yml`, `microservice_admin/src/app/api/health/route.ts`: по live-инциденту с `redpanda:29092` добавлен readiness endpoint `GET /health/ready`, который реально проверяет Kafka bootstrap listener, а admin split/local probe и compose healthcheck переведены на него. Admin facade больше не отдаёт raw `500` при провале Kafka publish-path: transport failure нормализуется в structured `503 code=admin_kafka_unavailable`, reply timeout — в structured `504 code=admin_kafka_timeout`.
- `microservice_gateway/src/GatewayService.API/Kafka/KafkaRequestClient.cs`, `microservice_gateway/README.md`, `microservice_gateway/STRUCTURE.md`: после live-анализа split admin выяснилось, что backend facade жив, но любые Kafka-backed admin calls (`cmd.data.health`, `cmd.analytics.health`, `cmd.data.dataset.list_tables`) уходят в `504`. Усилен startup fallback gateway: если Kafka Admin create для `reply.gateway.{instanceId}` не подтверждается в budget, client всё равно продолжает с best-effort subscribe на reply inbox вместо перманентного состояния `reply inbox not ready`.
- `microservice_gateway/src/GatewayService.API/Kafka/KafkaRequestClient.cs`, `microservice_gateway/README.md`, `microservice_gateway/STRUCTURE.md`: найден live-инцидент не в `microservice_admin/page.tsx`, а в startup gateway. Reply inbox bootstrap для Kafka больше не блокирует запуск всего ASP.NET host: gateway теперь поднимает `/health` даже если Kafka/controller временно недоступны, а Kafka-facing requests ждут ready-state inbox в пределах своего timeout вместо crash-loop контейнера.
- `microservice_gateway/src/GatewayService.API/Kafka/IKafkaRequestClient.cs`, `Extensions/ServiceCollectionExtensions.cs`, `Market/DataServiceClient.cs`, `Market/DataTopics.cs`, `tests/GatewayService.UnitTests/Market/DataServiceClientTests.cs`: chart lazy hydrate больше не обходит `dataset_jobs`. Gateway теперь создаёт `ingest` job через `cmd.data.dataset.jobs.start`, ждёт terminal status через `cmd.data.dataset.jobs.get` и тем самым направляет first-hit hydrate в тот же data-service queue с cap `4` и per-table сериализацией по `target_table`. Hot-window cache `(symbol, timeframe, limit)` остаётся fast-path для частых запросов вроде `ETHUSDT + 15m + 100`.
- `microservice_admin/docker-compose.yml`, `microservice_admin/nginx/admin-online.conf.template`, `microservice_admin/.env.example`, `microservicestarter/start.*`, `microservicestarter/restart.*`: `onlyadmin` больше не публикует plain HTTP `443:3000`. Online-head теперь поднимает `admin-online` + `admin-online-proxy`: proxy слушает `80/443`, редиректит bare `/` на `/admin/`, использует cert/key с admin-host (`/etc/letsencrypt/live/sha-trade.tech/fullchain.pem`, `/etc/letsencrypt/live/sha-trade.tech/privkey.pem`) и по умолчанию обслуживает `https://sha-trade.tech/admin/` и `https://www.sha-trade.tech/admin/`. Launcher при пустых ключах дописывает эти доменные/TLS env defaults сам.
- `microservice_gateway/src/GatewayService.API/Market/ChartService.cs`, `IDataServiceClient.cs`, `DataServiceClient.cs`: `GET /api/v1/market/chart` больше не ограничивается first-hit ответом `pending` для валидного symbol/timeframe без локальной таблицы. Gateway теперь пытается синхронно догрузить нужное окно через `cmd.data.dataset.ingest`, перечитывает rows и старается вернуть свечи в том же HTTP-ответе; `pending`/`partial` остаются fallback при занятом ingest-lock, timeout/error или `claim_check`.
- `microservice_admin`: добавлена страница `/logs` шестым пунктом sidebar после Anomaly. Она читает process-local admin runtime log buffer через `/api/logs`, автообновляется, умеет запускать быстрый connectivity check и показывает source/event/level/details без token/payload.
- `microservice_admin/src/lib/adminRuntimeLog.ts`: server-only ring buffer последних admin diagnostics; `/api/health`, `/api/kafka` и `backendClient.ts` пишут туда те же этапы, которые печатают в container logs.
- Корневой `.gitignore` раньше глобально игнорировал любые каталоги `logs/`, из-за чего `microservice_admin/src/app/logs` и `src/app/api/logs` не попадали в git и давали `404` на admin-host при одинаковом `HEAD`. Добавлены точечные unignore-правила для этих route-каталогов.
- `microservice_admin/src/hooks/useDatasetJobs.ts` + `src/app/download/page.tsx`: добавлен fallback polling `cmd.data.dataset.jobs.list` / `cmd.data.dataset.jobs.get` для tracked ingest jobs. Это убирает UI-баг, где backend уже исполнял dataset jobs, а Download-экран оставался в ложном `queued/stalled`, если SSE не донёс progress/completed event.
- `microservice_data/src/DataService.API/Jobs/DatasetJobRunner.cs`: ingest concurrency поднят с 2 до 4 при сохранении отдельного heavy-TF gate на `1m`/`3m`. `microservice_admin/src/app/download/page.tsx` синхронизирован на 4 execution slot-а, а большой `Active dataset jobs` panel вынесен в новую вкладку `/queue`.
- `microservice_admin/src/app/queue/page.tsx` + `src/hooks/useDatasetJobsFeed.ts`: добавлена 7-я вкладка Queue. Она показывает live dataset jobs и request-stream из process-local admin runtime buffer в одном месте; Download теперь оставляет у себя только scoped ingest progress.
- `microservice_admin/src/hooks/useDatasetJobs.ts`, `src/app/api/kafka/route.ts`, `src/app/queue/page.tsx`: Queue history очищена от ping/polling шума и показывает только крупные операции с payload/response summary. Одновременно store dataset jobs теперь доразрешает пропавшие active jobs через `jobs.get`, чтобы completed job не зависала в active до ручного reload.

### 2026-05-18

- Split deployment стандартизирован на backend HTTPS facade `:8443`; прежняя схема обмена общим admin-ключом позднее заменена на admin JWT session.
- `microservicestarter` в `onlyadmin` собирал `ONLINE_*` и backend facade URL, а в `noadmin` собирал `PUBLIC_DOWNLOAD_BASE_URL` и `ADMIN_BACKEND_PORT`; token-sync часть позднее удалена.
- `microservicestarter` больше не отвечает за общий admin-ключ: актуальная схема использует login-only admin account и JWT.
- Bash launcher (`microservicestarter/start.sh`, `restart.sh`) теперь в финальном parallel summary перечисляет имена реально упавших сервисов, чтобы на Linux backend-host было проще отличать общий fail от конкретного виновника.
- Launcher теперь в `restart` автоматически восстанавливает отсутствующий `.env` из `.env.example`, а dangling image prune вынесен из child fan-out в один parent cleanup-pass, чтобы parallel deploy не падал на `a prune operation is already running`.
- `microservice_analitic/docker-compose.yml`: встроенный Redis больше не публикуется на host `6379`; он остаётся только внутренней зависимостью `api`/`scheduler`, чтобы backend-host не ловил port conflict при auto-deploy.
- Launcher cleanup усилен межпроцессным lock-ом вокруг `docker image prune`, чтобы даже при нескольких параллельных child-процессах cleanup не конфликтовал между собой.
- Старый split-transport код, compose-сервисы и отдельные markdown-гайды удалены из активной схемы.
- Актуальные операционные документы: root `README.md`, `STRUCTURE.md`, `microservicestarter/README.md`, `microservice_admin/README.md`, `microservice_infra/README.md`.
- `microservice_admin/src/app/api/health/route.ts`: split-mode health response снова совпадает с `InfraHealthResponse`; `npm run build` для admin проходит.
- `microservice_infra/docker-compose.yml`: backend HTTPS facade на `:8443` больше не падает без ручных `tls.crt` / `tls.key`; one-shot `nginx-cert-init` автогенерирует self-signed cert в `ADMIN_BACKEND_CERTS_DIR`, если каталог пустой.
- `microservice_admin/docker-compose.yml` + `src/lib/backendClient.ts`: split admin-head получил `ADMIN_BACKEND_TLS_INSECURE`; `admin-online` по умолчанию принимает autogenerated self-signed backend cert, а trusted-cert сценарий остаётся через `ADMIN_BACKEND_TLS_INSECURE=0`.
- Актуальные env/docs очищены от legacy transport-формулировок: в живых конфигурациях split deployment больше описывается только через backend facade `:8443` и обычные private/public IP или DNS.
- `microservicestarter/start.sh` и `restart.sh`: bash launcher теперь заранее проверяет publish-ports (`8443`, `8501`, `7510`, `7520`, infra ports, `ADMIN_HTTP_PORT`, `ADMIN_HTTPS_PORT`) и падает с явным сообщением, если их держит чужой контейнер/процесс. `stop.sh` по-прежнему управляет только compose-проектами ModelLine и не удаляет сторонние контейнеры.
- `microservice_infra/nginx/nginx.conf`: split facade `/api/admin/*` был сломан alias-ом `gateway:5020`; реальный upstream в сети `modelline_net` — `gateway-service:5020`. Из-за этого `8443` мог отдавать `502`, даже когда прямой `http://<host>:7520/health` уже был healthy.
- `microservicestarter` preflight check на порты в bash-скриптах не должен ломать CI/CD restart: сравнение container IDs теперь учитывает long/full IDs и считает уже работающие контейнеры того же compose-проекта своими, а не внешними.
- `microservicestarter` preflight parser теперь понимает Docker publish ranges (`9000-9001->9000-9001`), иначе свой `minio` не находился через `docker ps`, а `ss` видел только `docker-proxy` и ложноположительно блокировал повторный `restart`.
- Split admin diagnostics усилены: admin `backendClient`/`/api/kafka` пробрасывает `status`, `code`, `detail`, `correlationId` и отдельно классифицирует `401/403`, `502`, `504` и network failures.
- Новый backend token `1d6a064897cb2cec765f3538807471b833d64fac39f16214540fc35b6bbbd67e` проходит auth на `https://95.165.27.159:8443/api/admin/*`; текущий live-блокер сместился с `401` на симметричный `504 Kafka request timed out` для `cmd.data.health` и `cmd.analytics.health`.
- `microservice_gateway/src/GatewayService.API/Kafka/KafkaRequestClient.cs`: reply-inbox `reply.gateway.{instanceId}` теперь создаётся явно через Kafka Admin API до `Subscribe`, с retry на startup. Это закрывает гонку, когда gateway healthy по HTTP, но admin facade зависает на request/reply timeout из-за неготового reply topic.
- После backend redeploy `cmd.data.health` через `:8443` отвечает `200`; оставшаяся admin-host проблема — TLS trust/runtime env: Node без `ADMIN_BACKEND_TLS_INSECURE=1` падает на self-signed cert как `DEPTH_ZERO_SELF_SIGNED_CERT`. Admin diagnostics теперь отдаёт `admin_backend_tls_untrusted`, `/api/health` вычисляет split-mode из runtime env, а launcher пишет `ADMIN_BACKEND_TLS_INSECURE=1` для пустого https facade.
- Добавлены подробные split-логи без секретов: admin tags `[api/health]`, `[api/kafka]`, `[admin-backend]`; gateway logs `AdminFacade request ...` и `KafkaRequest ...`. Сопоставлять по `correlationId`, смотреть branch/env, TLS flag, URL/path/topic, status/code/detail и duration.
