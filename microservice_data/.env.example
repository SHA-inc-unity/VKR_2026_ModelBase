PGHOST=postgres
PGPORT=5433
PGDATABASE=crypt_date
PGUSER=postgres
PGPASSWORD=postgres

DATA_API_HOST=0.0.0.0
DATA_API_PORT=8100

# Browser-facing origin for signed download URLs (/modelline-blobs/*).
# local/full stack: leave the default infra-nginx origin.
# split deployment with backend facade: set to https://<backend-host>:8443
PUBLIC_DOWNLOAD_BASE_URL=http://localhost:8501
