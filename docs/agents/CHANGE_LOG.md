# Agent Change Log

Журнал коротких записей о заметных изменениях, которые влияют на разработку, маршруты чтения документации и агентный workflow.

## 2026-05

### 2026-05-23

- `microservice_gateway/src/GatewayService.API/Kafka/KafkaRequestClient.cs`, `microservice_gateway/{README.md,STRUCTURE.md}`, `microservice_infra/{README.md,STRUCTURE.md}`: закрыт live incident с `Kafka error: HTTP 503` на split admin. Root cause был не в broker bootstrap, а в том, что `redpanda-janitor` удалял живой, но ещё пустой `reply.gateway.*` topic с `HW=0`; gateway после `partitions revoked` оставался `kafka-request-reply=Unhealthy` до рестарта. `KafkaRequestClient` теперь даже после успешного `CreateTopicsAsync` сразу пишет bootstrap-marker в новый reply inbox, чтобы `HighWatermark > 0` до первого реального reply. После rebuild/redeploy gateway `/health/ready` и backend facade `:8443/health/ready` вернулись к `200`.

- `microservice_data/src/DataService.API/{Jobs/MarketWatcherService.cs,Database/DatasetRepository.cs}`, `microservice_analitic/backend/{data_client.py,anomaly/session.py,anomaly/isolation_forest.py}`, `microservice_admin/src/app/anomaly/page.tsx`, `microservice_analitic/tests/test_anomaly_session_projection.py`, `microservice_data/{README.md,STRUCTURE.md,EXCHANGE_APIS.md}`, `microservice_analitic/{README.md,STRUCTURE.md}`, `microservice_admin/{README.md,STRUCTURE.md}`: закрыт combined incident/performance slice по stale market watcher и slow anomaly. Data-service watcher теперь на каждом candle rollover зеркалит closed O/H/L/C в canonical dataset tables через новый `BulkUpsertLiveCandlesAsync`, а не обновляет только `market_watch_live`; это вернуло `btcusdt_1m` к near-realtime после rebuild/redeploy. Analytics service получил timestamp normalization (`timestamp_utc` ↔ `timestamp_ms`) и небольшой session-level parquet read-cache в `backend/anomaly/session.py`, поэтому DBSCAN/Isolation Forest больше не падают на реальном session parquet и повторные anomaly-запуски не перечитывают один и тот же slice с диска. Admin `/anomaly` ускорен summary-first orchestration: initial Analyze теперь делает `column_stats(count_only=true)` и догружает полный profile в фоне. Runtime validation выполнена после rebuild обоих сервисов.

- `microservice_gateway/src/GatewayService.API/{Controllers/AccountController.cs,Controllers/MarketController.cs,Controllers/PortfolioController.cs,Controllers/ExchangesController.cs,Controllers/AlertsController.cs,Controllers/ServiceTogglesController.cs,Controllers/MobileAdminController.cs,Clients/Account/*,Clients/{Market,News,Notifications,Portfolio}/*,DTOs/Requests/FrontendContractsRequests.cs,DTOs/Responses/FrontendContractsResponses.cs,Frontend/*,Extensions/ServiceCollectionExtensions.cs}`, `microservice_account/src/AccountService.Application/{DTOs/Requests/LoginRequest.cs,DTOs/Responses/AuthResponse.cs,Services/AccountAppService.cs,Validators/LoginRequestValidator.cs}`, `microservice_account/tests/AccountService.UnitTests/Services/AccountAppServiceTests.cs`, `microservice_gateway/{README.md,API.md,STRUCTURE.md}`, `microservice_account/{README.md,STRUCTURE.md}`: frontend-contract gap по `API_requests.md` закрыт на BFF/account уровне. Gateway теперь публикует auth passthrough `POST /api/account/{register,login,refresh,logout}`, public `GET /api/v1/market/overview`, detailed `GET /api/portfolio/summary`, `/api/exchanges/*`, `/api/alerts/*`, `/api/services/toggles` и lightweight mobile-admin `GET /api/admin/{summary,users,services,statistics}`. Эти новые personal/mobile payload-ы пока строятся через gateway-local in-memory fallback state и не переживают restart. В `microservice_account` login теперь принимает `email` или `login`, а auth responses возвращают top-level `id` и `email` вместе с `uid`.

- `microservice_data/src/DataService.API/{Settings/DataServiceSettings.cs,Jobs/MarketWatcherService.cs,appsettings.json}`, `microservice_data/{README.md,STRUCTURE.md,EXCHANGE_APIS.md}`: по operator-request Kraken убран из default live watcher-а. Root cause текущего шума был не в stale UI, а в том, что default `MarketWatch.Exchanges` всё ещё включал `kraken`, поэтому после рестарта watcher стабильно выходил в `running + degraded` с `429 RateLimitRequest` и лишней ошибкой в status. Default/fallback live exchanges теперь `bybit` + `binance`; Kraken оставлен только как manual opt-in path. Проверка после rebuild/redeploy через `/api/admin/market-watcher/{status,rows}`: `status=running`, `message="Watching 34 symbols across binance, bybit"`, `exchanges=["bybit","binance"]`, `liveRows=34`, `lastError=null`, Kraken rows отсутствуют.

### 2026-05-22

- `microservice_data/src/DataService.API/{Jobs/MarketWatcherService.cs,Jobs/MarketWatcherRuntimeState.cs}`, `microservice_data/{README.md,STRUCTURE.md,EXCHANGE_APIS.md}`: закрыт следующий safety-slice после достижения `3 exchanges ~1s`. На runtime выяснилось, что occasional Kraken websocket startup `429` (`RateLimitRequest`) может выбить весь watcher, хотя Binance и Bybit уже живы. Startup semantics для watcher смягчены точечно: Kraken rate-limit после активных healthy venues теперь переводит watcher в `running` + degraded message вместо full failure, а stale Kraken rows сразу prune-ятся из in-memory/runtime DB state. Валидация после rebuild/redeploy через `/api/admin/market-watcher/{status,rows}`: `status=running`, `trackedSymbols=34`, `liveRows=34`, Kraken rows не остаются stale, fallback venues держат healthy lag (`binance avg ~361 ms`, `bybit avg ~752 ms`).

- `microservice_data/src/DataService.API/{Jobs/MarketWatcherService.cs,Markets/KrakenApiClient.cs}`, `microservice_data/{README.md,STRUCTURE.md,EXCHANGE_APIS.md}`: добит финальный runtime slice по требованию «3 биржи и lag около 1s или ниже». `MarketWatcherService` получил per-exchange startup retries с all-or-nothing semantics, Kraken переведён на managed `KrakenSpotSymbolOrderBook` path с synced top-of-book midpoint и коротким refresh-loop'ом для тихих пар. Финальная проверка через gateway admin endpoints дала steady-state `status=running`, `binance avgLagMs ~124`, `bybit ~323`, `kraken ~251`, общий `maxLagMs ~919`, то есть все три биржи одновременно снова укладываются в subsecond runtime budget.

- `microservice_data/src/DataService.API/{Jobs/MarketWatcherService.cs,Markets/KrakenApiClient.cs}`, `microservice_data/{README.md,STRUCTURE.md,EXCHANGE_APIS.md}`: закрыт runtime defect, из-за которого `kraken` пропадал из `/market-watcher` после backend-only рестарта. Root cause оказался в startup discovery: watcher делал полный `Kraken /AssetPairs` fetch под жёстким deadline и затем ещё пытался подписываться на Kraken `wsname`, где `BTCUSDT` приходил как `XBTUSDT/XBT/USDT` и ломал websocket subscription. Discovery переведён на narrow whitelist path `pair=BASE/USDT`, unsupported pairs quietly skip-аются, а realtime subscription использует canonical `BASE/USDT` alias. В runtime-проверке после redeploy `market_watch_live` снова содержит `kraken` rows, то есть третья биржа больше не выпадает целиком из live universe.

- `microservice_data/src/DataService.API/{Database/DatasetJobsMutator.cs,Jobs/DatasetJobRunner.cs}`, `microservice_data/{README.md,STRUCTURE.md}`: startup recovery теперь добирает последний legacy хвост после отвязки watcher-а от queue. Data-service физически удаляет все старые `dataset_jobs.type='market_watch'` rows, поэтому `cmd.data.dataset.jobs.list` и operator Queue больше не видят historical failed watcher-попытки как часть актуальной очереди.

- `microservice_data/src/DataService.API/Jobs/MarketWatcherService.cs`, `microservice_data/{README.md,STRUCTURE.md,EXCHANGE_APIS.md}`: для live Market Watcher переключён источник liveness-price на более частые book-side streams. Binance больше не сидит на broad USD-M all-ticker feed: watcher открывает targeted book-ticker subscriptions только по whitelist symbols и считает live price как midpoint best bid/ask. Bybit переведён с linear ticker на depth-1 orderbook, тоже с midpoint best bid/ask. Это сохранило websocket-only модель без лишних DB writes на каждый tick и дало реальный runtime сдвиг после redeploy: protected `/admin/api/kafka` check показал `avgLagMs` примерно `5438 -> 400`, `maxLagMs` примерно `51006 -> 3552`, при том что Kraken по-прежнему деградирует отдельно, если discovery превышает `30 s`.

- `microservice_data/src/DataService.API/{Jobs/MarketWatcherService.cs,Database/MarketWatchRepository.cs,Settings/DataServiceSettings.cs,Dataset/DatasetConstants.cs,appsettings.json}`, `microservice_data/{README.md,STRUCTURE.md}`: live market watcher больше не подписывается на весь tradable exchange universe. Discovery теперь фильтруется по `DataService:MarketWatch:Symbols`; дефолтный whitelist синхронизирован с символами из Admin `Dataset Configuration`, поэтому `/market-watcher` показывает только поддерживаемый dataset universe, а не все инструменты бирж. Дополнительно startup discovery каждого exchange ограничен отдельным дедлайном, чтобы занятый Kraken limiter или подвисший upstream не держал watcher бесконечно в `starting`, а переводил сервис в `degraded` с fallback на persisted symbols. После расчёта текущего universe сервис prune-ит stale rows из `market_watch_live`, а Binance all-ticker callback локально игнорирует non-whitelist symbols, поэтому UI и runtime state больше не разъезжаются по старому symbol scope. Валидация: `dotnet build src/DataService.API/DataService.API.csproj`, runtime check через `/admin/market-watcher`.

- `microservice_admin/src/lib/backendClient.ts`, `microservice_gateway/src/GatewayService.API/{Controllers/AdminController.cs,Kafka/AdminTopics.cs}`, `microservice_admin/STRUCTURE.md`, `microservice_gateway/{README.md,API.md,STRUCTURE.md}`: закрыт split-mode regression для страницы `Market Watcher`. После вынесения watcher-а в отдельные Kafka topics local mode работал, но admin backend facade не знал новые команды `cmd.data.market_watcher.{status,set_enabled,rows,logs}`. Добавлены `TOPIC_PATH` mapping в admin, новые `POST /api/admin/market-watcher/{status,set-enabled,rows,logs}` routes в gateway и соответствующие `AdminTopics`, поэтому watcher page теперь работает и через backend-host, а не только при прямом Kafka доступе.

- `microservice_data/src/DataService.API/{Jobs/MarketWatcherService.cs,Jobs/MarketWatcherRuntimeState.cs,Database/MarketWatchRepository.cs,Kafka/KafkaConsumerService.cs,Kafka/Topics.cs,Program.cs,Jobs/JobHandlers.cs,Jobs/DatasetJobRunner.cs,Database/DatasetJobsMutator.cs}`, `microservice_admin/src/{app/market-watcher/page.tsx,components/Sidebar.tsx,lib/topics.ts,lib/i18n.ts,hooks/useDatasetJobs.ts,app/queue/page.tsx,lib/types.ts}`, `microservice_data/{README.md,STRUCTURE.md}`, `microservice_admin/{README.md,STRUCTURE.md}`: queue-visible `market_watch` убран из `dataset_jobs` и заменён на dedicated hosted runtime `MarketWatcherService` внутри data-service. Управление/наблюдение теперь идут по отдельным Kafka topics `cmd.data.market_watcher.{status,set_enabled,rows,logs}`; admin получил 8-ю страницу `Market Watcher` с enable/disable, realtime prices, lag и watcher-only logs. Queue History больше не включает watcher traffic, а `Duration` для terminal dataset jobs считается как `started_at → finished_at`. Дополнительно ingest-path получил cheap coverage short-circuit перед `FindMissingTimestampsAsync`, чтобы no-op high-TF окна не зависали на долгом empty-check.

- `microservice_data/src/DataService.API/{Jobs/MarketWatchBootstrapService.cs,Jobs/MarketWatchJobHandler.cs,Database/MarketWatchRepository.cs,Database/DatasetJobModels.cs,Jobs/DatasetJobRunner.cs,Program.cs,Settings/DataServiceSettings.cs,Markets/IMarketDataClient.cs,Bybit/BybitApiClient.cs,Markets/BinanceApiClient.cs,Markets/KrakenApiClient.cs,DataService.API.csproj}`, `microservice_data/{README.md,STRUCTURE.md,EXCHANGE_APIS.md}`: data-service получил perpetual `market_watch` path. Новый hosted bootstrap гарантирует один queue-visible singleton job `market_watch`, handler открывает websocket ticker streams через `Binance.Net` / `Bybit.Net` / `KrakenExchange.Net`, локально агрегирует current candles по настроенным TF и сохраняет live overlay в отдельную таблицу `market_watch_live` (one row per `exchange + symbol`, candles packed in `jsonb`). Исторические dataset tables больше не нужны для live polling и не конфликтуют с этим worker-ом; current Kraken realtime scope пока сознательно ограничен `*USDT` spot universe.

- `microservice_data/src/DataService.API/{Bybit/BybitApiClient.cs,Jobs/JobHandlers.cs,Dataset/DatasetConstants.cs}`, `microservice_data/{README.md,STRUCTURE.md,EXCHANGE_APIS.md}`: проведён такой же benchmark/tune cycle для Bybit historical fetch. Сначала найден локальный parse defect: `BybitApiClient.FetchInstrumentDetailsAsync` ожидал numeric `launchTime`, хотя `/v5/market/instruments-info` для `BTCUSDT` возвращает строку; parsing переведён на tolerant string-or-number path. Затем реальный benchmark на 1,000,000 свечей (`BTCUSDT`, 1m) показал, что узкое место — не merge-path, а слишком низкий heavy kline fan-out: при переходе с `2` на `6` время сократилось примерно с `2:17` до `0:51`. Production `IngestJobHandler` теперь использует `MaxParallelApiWorkersBybitHeavy=6`. В том же цикле Kraken benchmark не удалось довести до стабильной базы: даже прямой host `curl` на reachable `OHLC` window (`interval=60`) таймаутился за `15 s` с частично полученным body, поэтому Kraken tuning в этом срезе не публиковался.

- `microservice_data/src/DataService.API/{Jobs/JobHandlers.cs,Dataset/DatasetConstants.cs,Markets/BinanceApiClient.cs,Markets/BinanceRateLimiter.cs}`, `microservice_data/{README.md,STRUCTURE.md,EXCHANGE_APIS.md}`: ускорен Binance historical fetch path без ослабления scheduler heavy-gate. Для heavy `1m`/`3m` Binance jobs сохранена сериализация на уровне scheduler, но внутри job page fan-out поднят до `6`, shared `BinanceRateLimiter` ускорен до `6` inflight / `35 ms` per unit, а `BinanceApiClient` перестал склеивать million-candle pages через полный dictionary+sort проход. Реальный benchmark по 1,000,000 свечей (`BTCUSDT`, 1m) показал сокращение fetch времени примерно с `11:14` до `3:56` (`~2.85x`).

- `microservice_data/src/DataService.API/Markets/{BinanceApiClient.cs,KrakenApiClient.cs}`, `microservice_data/{README.md,STRUCTURE.md,EXCHANGE_APIS.md}`: закрыт новый binance/kraken ingest incident из dataset job logs. `BinanceApiClient` перестал отправлять stale `startTime` в `/futures/data/openInterestHist`: local retention clamp сужен с 31 до strict 30 days и остаётся выровненным по границе периода, поэтому Binance больше не должен отвечать `400 parameter 'startTime' is invalid` на старые OI окна. `KrakenApiClient` получил safe fast-path для OHLC: если клипованное окно помещается в reachable 720-candle history, client сначала делает один запрос на весь диапазон и только при неполном ответе откатывается к прежнему throttled paged fetch. Валидация: `dotnet build src/DataService.API/DataService.API.csproj`.

- `microservice_admin/src/app/download/page.tsx`, `microservice_admin/{README.md,STRUCTURE.md}`: Dataset Download получил `Exchange=ALL` как aggregate ingest-only режим. Admin теперь fan-out'ит выбор по `exchange × symbol × timeframe`, а tracked jobs и `AllIngestProgress` используют full-scope key `exchange::symbol::timeframe`, чтобы одинаковые TF с разных бирж не коллидировали. Для aggregate-режимов single-table actions (`coverage/export/repair/delete`) intentionally отключены. Валидация: TS diagnostics чистые, `docker build -t microservice-admin-exchange-all-check .` проходит.

- `microservice_admin/src/{app/download/page.tsx,lib/constants.ts}`, `microservice_admin/{README.md,STRUCTURE.md}`: Dataset Download получил `Symbol=ALL` как orchestration-only режим массовой загрузки. Admin теперь fan-out'ит выбранную биржу в набор обычных ingest jobs по всем `symbol::timeframe`, а `AllIngestProgress` показывает эти jobs по full-scope label вместо чистого TF. Для `Symbol=ALL` intentionally отключены coverage/export/repair/delete, потому что эти операции адресуют одну таблицу. Валидация: TS diagnostics по изменённым файлам чистые, `docker build -t microservice-admin-all-datasets-check .` проходит.

- `microservice_data/src/DataService.API/{Database/DatasetJobModels.cs,Database/DatasetJobsRepository.cs,Database/DatasetJobsMutator.cs,Jobs/IDatasetJobHandler.cs,Jobs/JobHandlers.cs,Jobs/DatasetJobRunner.cs,Kafka/KafkaConsumerService.Jobs.cs,Markets/BinanceApiClient.cs,Markets/BinanceRateLimiter.cs,Program.cs}`, `microservice_admin/src/{lib/types.ts,hooks/useDatasetJobs.ts,components/DatasetJobsPanel.tsx}`, `microservice_data/{README.md,STRUCTURE.md}`, `microservice_admin/{README.md,STRUCTURE.md}`: dataset jobs progress переведён на dual-model контракт. Data-service теперь публикует и хранит не только weighted overall `progress`, но и current-stage snapshot (`stage_progress`, `stage_total`, `stage_completed`, `stage_failed`, `stage_skipped`), а admin Queue рендерит раздельные stage/overall progress bars. В том же срезе закрыт текущий binance ingest slowdown: root cause был не в scheduler concurrency, а в upstream `418` IP ban для `/fapi/v1/klines` и `400 parameter 'startTime' is invalid` для `/futures/data/openInterestHist`. `BinanceApiClient` получил process-local limiter с `Retry-After` penalty, а OI fetch теперь клипуется к latest-month retention и выравнивается по границе периода. Валидация: `dotnet build DataService.sln`, `docker compose build admin admin-online`.

- `microservice_admin/{Dockerfile,.dockerignore,README.md,STRUCTURE.md}`: ускорен repeat-build path admin-образа. Build stage получил именованные BuildKit cache mounts для npm и `.next/cache`, `next build` теперь запускается с `NODE_COMPILE_CACHE`, а `.dockerignore` перестал отправлять в Docker context сервисные Markdown и proxy-конфиги, не влияющие на standalone image. Валидация: `docker build -t microservice-admin-cache-check .` проходит; повторный build берёт `npm ci` и `next build` из cache.

- `microservice_admin/src/{app/page.tsx,lib/i18n.ts}`, `microservice_admin/{README.md,STRUCTURE.md}`: Dashboard dataset-summary стал пригоднее для multi-exchange. Добавлен exchange dropdown по реально доступным непустым биржам, `Coverage` chart поднят по высоте и теперь строится только по видимым non-zero tables, а `Tables` и `Total Tables` больше не показывают и не считают строки с `0 rows`. Валидация: TS diagnostics чистые, `docker compose build admin` и `docker compose build admin-online` проходят.

- `microservice_data/src/DataService.API/{Markets/KrakenApiClient.cs,Markets/KrakenRateLimiter.cs,Program.cs}`, `microservice_data/{README.md,STRUCTURE.md,EXCHANGE_APIS.md}`: закрыт live regression после per-exchange 4-slot ingest pool для Kraken. Root cause был не в scheduler-е, а в public REST path: несколько Kraken jobs одновременно запускали paged OHLC fetch и быстро ловили `EGeneral:Too many requests`. В Kraken client добавлен process-local limiter (общий inflight gate + inter-request spacing) и retry/backoff на Kraken-specific throttle ошибки (`Too many requests`, `Rate limit exceeded`, `Throttled: <unix>`). Live validation после rebuild: четыре `cmd.data.dataset.jobs.start` для `kraken_btcusdt_{120m,240m,720m,1d}` были приняты; `120m` и `240m` завершились `succeeded`, а `720m` и `1d` остались healthy `running` вместо мгновенного terminal fail.

- `microservice_data/src/DataService.API/{Jobs/JobHandlers.cs,Markets/IMarketDataClient.cs,Markets/BinanceApiClient.cs,Bybit/BybitApiClient.cs,Markets/KrakenApiClient.cs}`, `microservice_data/{README.md,STRUCTURE.md}`: проверен live incident со «застывшими» dataset jobs на `fetch_oi`. Root cause оказался не в deadlock/job-runner, а в progress-модели: jobs в Postgres продолжали heartbeat (`updated_at` age ~2–3 s), но `IngestJobHandler` держал фиксированный `47%`, потому что market client interface умел page-progress только для `fetch_klines`. В `FetchOpenInterestAsync` добавлен optional `onPageDone`, Bybit/Binance теперь репортят completed OI pages, а handler публикует промежуточный `fetch_oi` progress вместо вечного `waiting for open interest`.

- `microservice_data/src/DataService.API/{Jobs/JobHandlers.cs,Markets/KrakenApiClient.cs}`, `microservice_admin/src/{hooks/useDatasetJobs.ts,app/download/page.tsx,app/api/queue/history/route.ts,components/DatasetJobsPanel.tsx}`, `microservice_data/EXCHANGE_APIS.md`, `microservice_data/{README.md,STRUCTURE.md}`, `microservice_admin/{README.md,STRUCTURE.md}`, `docs/agents/services/microservice_data.md`: закрыт следующий срез после ingest-starvation. Kraken больше не тянет полный `/0/public/AssetPairs` catalog на каждый symbol resolve: client использует узкий `AssetPairs?pair=...`, а полностью недостижимые historical windows завершаются как no-op вместо красных runtime-failures. В admin live dataset jobs `failed/canceled` автоубираются через 10 секунд, terminal completions best-effort пишутся в SQLite-backed queue history через `POST /api/queue/history`, а Download-page одновременно сохраняет ingest errors в локальный Action History, чтобы transient errors не раздували layout. Добавлен `microservice_data/EXCHANGE_APIS.md` — короткая памятка по Kraken / Bybit / Binance API и их реальным ограничениям для ModelLine.

- `microservice_data/src/DataService.API/{Jobs/DatasetJobRunner.cs,Jobs/JobHandlers.cs,Kafka/KafkaProducer.cs,Markets/KrakenApiClient.cs,Markets/BinanceApiClient.cs,Bybit/BybitApiClient.cs}`, `microservice_data/{README.md,STRUCTURE.md}`: закрыт dataset ingest starvation/stall вокруг Kraken. Root cause был двойной: один общий ingest cap `4` позволял одной бирже занять все runner slots, а progress publish ждал Kafka delivery и мог подвесить job handler на `fetch_klines`. Scheduler переведён на `4` ingest-slot'а на каждую биржу + отдельный heavy gate `1` на `1m`/`3m` для каждой биржи, progress events стали best-effort, Kraken ingest теперь клипует request window к reachable history (последние 720 base candles) и fast-fail'ит полностью недостижимые окна, а Bybit/Binance/Kraken clients читают response body целиком под общим HTTP timeout. Runtime validation: `dotnet build src/DataService.API/DataService.API.csproj` проходит, rebuilt `microservice_data` healthy, requeued `bybit` `720m` job завершается сразу даже при одновременно running `kraken` `720m`, то есть биржи больше не блокируют друг друга общим ingest-пулом.

- `microservice_account/src/AccountService.API/{Extensions/ServiceCollectionExtensions.cs,Extensions/MigrationExtensions.cs}`, `microservice_account/{README.md,STRUCTURE.md}`, `docs/agents/services/microservice_account.md`: разобран и закрыт live login incident admin-панели. Реальная причина `fetch failed` оказалась не в `microservice_admin`, а в crash-loop `account_service_api`: runtime приходил к пустой application schema, видел только `__EFMigrationsHistory`, после чего bootstrap-admin падал на `relation "roles" does not exist`. Startup migration теперь логирует discovered/pending migrations, а если schema полностью пустая, сам восстанавливает core tables из текущей EF-модели и продолжает bootstrap-admin seed; partial schema drift по-прежнему остаётся hard-fail. После rebuild контейнера `account_service_api` и `microservice_admin` `POST /admin/api/auth/login` снова отвечает `200` для `admin/admin`.
- `microservicestarter/{start.sh,restart.sh}`, `microservicestarter/{README.md,STRUCTURE.md}`: bash launcher для `onlyadmin` теперь не требует ручной правки `microservice_admin/.env` после выбора backend host. Derived split endpoints зеркалятся ещё и в direct runtime env (`ACCOUNT_URL`, `GATEWAY_URL`, `REDPANDA_ADMIN_URL`, `MINIO_URL`, `KAFKA_BOOTSTRAP_SERVERS`, `BACKEND_CONNECTION_TARGET`), а `start.sh` / `restart.sh` печатают полный итоговый admin env summary в консоль.
- `microservice_account/src/AccountService.API/Extensions/MigrationExtensions.cs`, `tests/AccountService.UnitTests/Extensions/MigrationExtensionsTests.cs`, `microservice_account/{README.md,STRUCTURE.md}`: bootstrap-admin на startup больше не опирается только на email. На каждом запуске контейнера Account Service теперь проверяет наличие bootstrap username (`admin` по умолчанию), создаёт пользователя, если его нет, и при необходимости привязывает этот username к уже существующему bootstrap email user. Добавлены focused unit tests на create/update сценарии.
- `microservice_account/src/AccountService.API/Extensions/MigrationExtensions.cs`, `src/AccountService.Application/{DTOs/Requests/LoginRequest.cs,Validators/LoginRequestValidator.cs,Services/AccountAppService.cs,Interfaces/Repositories/IUserRepository.cs}`, `src/AccountService.Infrastructure/Repositories/UserRepository.cs`, `microservice_account/{.env.example,README.md,STRUCTURE.md}`, `microservice_admin/{README.md,STRUCTURE.md}`: первый bootstrap users table теперь поднимает дефолтного admin-пользователя `admin/admin`, если `AdminBootstrap:*` полностью пуст. Account login больше не email-only: endpoint принимает email или username, поэтому admin-панель может входить по логину `admin` без отдельного email. Кастомный bootstrap через env по-прежнему поддерживается и должен переопределять дефолтные credentials в production.
- `microservice_admin/src/middleware.ts`, `microservice_admin/{README.md,STRUCTURE.md}`, `docs/agents/services/microservice_admin.md`: admin login cache доведён до полноценной session persistence. Middleware теперь не только прячет панель за `/login`, но и восстанавливает admin access token через `POST /api/account/refresh`, если в httpOnly cookie ещё жив refresh token. Защищённые pages/API получают уже обновлённый `cookie` header, а `/login` редиректится назад в панель, если cached admin-session ещё можно восстановить.

### 2026-05-21

- `microservice_account`, `microservice_gateway`, `microservice_admin`, `microservicestarter`, root/docs: начат keyless auth-refactor admin path. Account получил роль `guest`, top-level UID/accountType/roles в auth response и optional bootstrap login-only admin. Gateway `/api/admin/*` теперь защищён Account Service JWT с ролью `admin`, старый фильтр общего ключа удалён. Admin UI получил `/login`, auth/session/logout routes, middleware и передачу admin JWT в split backend facade. Launcher больше не генерирует и не синхронизирует общий admin-ключ; устаревшие token helper scripts удалены.
- `microservice_gateway/src/GatewayService.API/Kafka/{IKafkaRequestClient.cs,KafkaRequestClient.cs,KafkaRequestReplyHealthCheck.cs}`, `src/GatewayService.API/Program.cs`, `tests/GatewayService.UnitTests/KafkaRequestReplyHealthCheckTests.cs`, `microservice_gateway/{README.md,API.md,STRUCTURE.md}`: gateway reply-inbox readiness стал диагностироваться заметно точнее. Добавлен `ReplyInboxStatus` — последний bootstrap/subscribe/assignment state request/reply path; он попадает в `KafkaRequestReplyHealthCheck`, в `/health/ready` JSON body и в detail structured `504 admin_kafka_timeout`. Одновременно wait на not-ready reply inbox ограничен коротким budget, поэтому split admin чаще получает быстрый backend `504` с причиной вместо generic client-side `admin_backend_timeout` после 10 s HTTP abort.
- `deploy/modelline-deploy.yml`, `deploy/reconcile.sh`, `README.md`, `STRUCTURE.md`, `microservice_gateway/README.md`, `microservice_gateway/STRUCTURE.md`: обнаружен и исправлен дополнительный backend deployment drift, который маскировал split-admin incident как "правка в repo есть, но live её не видит". Root deploy config ссылался на несуществующие compose service names (`gateway`, `analitic`, `account`, `kafka/postgres/redis` для infra), а `reconcile.sh` под `set -e` аварийно завершался уже на втором YAML entry из-за `(( idx++ ))`. Теперь deploy config синхронизирован с реальными compose services (`gateway-service`, `data`, `api`, `account-api`, `redpanda`, `redpanda-console`, `minio`, `nginx`, `redpanda-janitor`), infra reconcile делает explicit restart для применения bind-mounted nginx config, а dry-run по всему deployment graph проходит успешно.
- `deploy/{print_token.sh,set_token.sh}`, `README.md`, `STRUCTURE.md`, `microservice_gateway/README.md`, `microservice_gateway/STRUCTURE.md`: ранний split admin token workflow был упрощён перед последующим удалением в пользу admin JWT session.
- `microservice_gateway/src/GatewayService.API/Kafka/{KafkaRequestClient.cs,IKafkaRequestClient.cs,KafkaRequestReplyHealthCheck.cs}`, `src/GatewayService.API/Program.cs`, `src/GatewayService.API/Extensions/ServiceCollectionExtensions.cs`, `tests/GatewayService.UnitTests/KafkaRequestReplyHealthCheckTests.cs`, `microservice_gateway/README.md`, `microservice_gateway/API.md`, `microservice_gateway/STRUCTURE.md`: устранён backend-side split-admin race в gateway request/reply path. Reply inbox `reply.gateway.{instanceId}` больше не считается ready сразу после `Subscribe`: gateway ждёт реальный consumer assignment, а если Kafka Admin create не подтвердил topic в startup budget, дополнительно bootstrap-ит его через producer publish в сам reply topic и повторяет recovery loop. Одновременно `/health/ready` перестал быть только bootstrap-ping'ом и теперь требует живой reply-inbox assignment через `KafkaRequestReplyHealthCheck`, чтобы unhealthy request/reply path больше не маскировался как ready.

### 2026-05-19

- `microservice_gateway/src/GatewayService.API/Controllers/DashboardController.cs`, `src/GatewayService.API/Aggregators/Dashboard/{IDashboardAggregator.cs,DashboardAggregator.cs}`, `tests/GatewayService.UnitTests/DashboardAggregatorTests.cs`, `tests/GatewayService.IntegrationTests/GatewayIntegrationTests.cs`, `microservice_gateway/README.md`, `microservice_gateway/API.md`, `microservice_gateway/STRUCTURE.md`: mobile guest-access model выровнен под фактическое требование Flutter API. `GET /api/dashboard` больше не требует регистрацию/JWT и работает как optional-auth endpoint: guest получает public market/news dashboard, а `portfolio` секция вообще не собирается и не маркируется degraded. Personal routes `GET /api/account/me` и `GET /api/notifications` остались protected. Заодно документация теперь явно фиксирует, что `guest` в mobile API — это anonymous gateway caller, а не persisted role в `microservice_account`.
- `microservice_gateway/src/GatewayService.API/Settings/CorsSettings.cs`, `src/GatewayService.API/Extensions/ServiceCollectionExtensions.cs`, `src/GatewayService.API/Program.cs`, `src/GatewayService.API/Controllers/AdminController.cs`, `src/GatewayService.API/appsettings*.json`, `tests/GatewayService.IntegrationTests/GatewayIntegrationTests.cs`, `microservice_gateway/README.md`, `microservice_gateway/API.md`, `microservice_gateway/STRUCTURE.md`: gateway получил browser-facing CORS policy для public/protected client routes. Web-fetch до `GET /api/news` и preflight `OPTIONS /api/dashboard` теперь проходят с `Access-Control-Allow-*`; `/api/admin/*` специально выведен из CORS через `DisableCors`, чтобы admin facade оставался server-to-server surface. Добавлены focused integration tests на cross-origin GET и auth preflight.
- `microservice_gateway/API.md`: HTTP-спецификация gateway синхронизирована с уже внесёнными gateway-изменениями. Документ теперь явно описывает `/api/admin/*` как backend facade для `microservice_admin`, фиксирует structured admin errors `admin_kafka_unavailable` / `admin_kafka_timeout`, split/local base URLs и route families, а также убирает устаревшую `empty` coverage semantics из `market/chart`.
- `microservice_data/src/DataService.API/Dataset/DatasetCore.cs`, `src/DataService.API/Markets/IMarketDataClient.cs`, `src/DataService.API/Markets/MarketDataClientFactory.cs`, `src/DataService.API/Markets/BinanceApiClient.cs`, `src/DataService.API/Markets/KrakenApiClient.cs`, `src/DataService.API/Bybit/BybitApiClient.cs`, `src/DataService.API/Program.cs`, `src/DataService.API/Jobs/JobHandlers.cs`, `src/DataService.API/Kafka/KafkaConsumerService.cs`, `src/DataService.API/Kafka/KafkaConsumerService.Jobs.cs`, `microservice_admin/src/lib/constants.ts`, `microservice_admin/src/app/download/page.tsx`, `microservice_admin/src/app/api/export/csv/route.ts`, `microservice_admin/README.md`, `microservice_admin/STRUCTURE.md`, `microservice_data/README.md`, `microservice_data/STRUCTURE.md`: dataset pipeline переведён на реальную multi-exchange модель. Data-service теперь маршрутизирует ingest/instrument-details через `bybit` / `binance` / `kraken`, canonical table naming стало exchange-aware (legacy Bybit `{symbol}_{timeframe}`, остальные `{exchange}_{symbol}_{timeframe}`), `jobs.start` и admin queue seed используют exchange-aware `target_table`, export `timeframe=ALL` собирается по выбранной бирже, а Download coverage cache/delete/export/repair parsing учитывают `exchange`. Quality repair пока честно ограничен Bybit tables, чтобы analytic path не чинил не ту таблицу.
- `microservice_admin/src/lib/sqliteStore.ts`, `microservice_admin/src/app/api/cache/route.ts`, `microservice_admin/src/lib/queueHistoryStore.ts`, `microservice_admin/src/app/download/page.tsx`, `microservice_admin/Dockerfile`, `microservice_admin/docker-compose.yml`, `microservice_admin/README.md`, `microservice_admin/STRUCTURE.md`, `microservice_data/src/DataService.API/Kafka/KafkaConsumerService.Jobs.cs`, `microservice_data/src/DataService.API/Jobs/JobHandlers.cs`, `microservice_data/README.md`, `microservice_data/STRUCTURE.md`: admin-panel state переведён на SQLite-backed persistence (`SQLITE_DB_PATH`, named volumes `admin_sqlite_data` / `admin_online_sqlite_data`). Queue history теперь хранится отдельно в SQLite, `/api/cache` стал SQLite bridge вместо Redis, Dataset Download получил inline exchange selector (`bybit/binance/kraken`) и persistent `modelline:params:dataset`, а data-service начал явно отвечать `unsupported_exchange` для ingest jobs не на `bybit`.
- `microservice_admin/src/lib/queueHistoryStore.ts`, `microservice_admin/src/app/api/queue/history/route.ts`, `microservice_admin/src/app/api/kafka/route.ts`, `microservice_admin/src/app/queue/page.tsx`, `microservice_admin/src/hooks/useDatasetJobsFeed.ts`, `microservice_admin/src/lib/i18n.ts`, `microservice_admin/README.md`, `microservice_admin/STRUCTURE.md`: Queue-page перестала читать filtered `/api/logs` как ложный "API / Server Request Stream". Введён отдельный queue-history store с Redis-backed best-effort persistence (`modelline:queue-history:v1`), `/api/kafka` пишет туда только крупные queue-topic operations, `/queue` читает уже `/api/queue/history`, а таблица стала реальной `Queue History` с duration, payload/response summary и error details. Одновременно Queue ускорен по live jobs: `useDatasetJobsFeed(1500)` и refresh на `focus` / `visibilitychange` убирают ощущение редких обновлений active jobs.
- `microservice_admin/src/app/api/health/route.ts`, `microservice_infra/nginx/nginx.conf`, `microservice_admin/README.md`, `microservice_admin/STRUCTURE.md`, `microservice_infra/README.md`, `microservice_infra/STRUCTURE.md`: readiness rollout сначала сломал split dashboard infra cards на старом live-facade: admin probe начал запрашивать `:8443/health/ready`, а nginx backend facade ещё публиковал только `= /health`, поэтому Redpanda/MinIO/account/gateway на dashboard ушли в `HTTP 404`, хотя Kafka-backed `cmd.data.health` и `cmd.analytics.health` оставались рабочими. Добавлен compatibility fallback `health/ready -> health` в admin route и backend nginx теперь публикует отдельный `GET /health/ready` как proxy в `gateway-service:5020/health/ready`.
- `microservice_gateway/src/GatewayService.API/Kafka/KafkaBrokerHealthCheck.cs`, `microservice_gateway/src/GatewayService.API/Program.cs`, `microservice_gateway/src/GatewayService.API/Controllers/AdminController.cs`, `microservice_gateway/src/GatewayService.API/DTOs/ErrorResponse.cs`, `microservice_gateway/docker-compose.yml`, `microservice_admin/src/app/api/health/route.ts`, `microservice_gateway/README.md`, `microservice_gateway/API.md`, `microservice_gateway/STRUCTURE.md`, `microservice_admin/README.md`, `microservice_admin/STRUCTURE.md`: после live-диагностики случая "gateway HTTP жив, но `redpanda:29092` не слушает" добавлен readiness endpoint `GET /health/ready`, который проверяет Kafka bootstrap listener. Docker healthcheck gateway и admin split/local health probe переведены на `/health/ready`, поэтому Kafka outage больше не маскируется как healthy. Admin facade transport-failures на Kafka publish-path теперь возвращают structured `503 code=admin_kafka_unavailable`, а timeout ожидания reply — structured `504 code=admin_kafka_timeout` вместо raw `500`.
- `microservice_gateway/src/GatewayService.API/Kafka/KafkaRequestClient.cs`, `microservice_gateway/README.md`, `microservice_gateway/STRUCTURE.md`: после live-diagnostics split admin backend выяснилось, что HTTP facade gateway доступен, но Kafka-backed команды таймаутятся. `KafkaRequestClient` получил дополнительный fallback: если Kafka Admin create не подтверждает `reply.gateway.{instanceId}` в пределах startup budget, gateway всё равно продолжает с best-effort subscribe на reply inbox, чтобы не держать все `cmd.*` запросы в вечном `reply inbox not ready`.
- `microservice_gateway/src/GatewayService.API/Kafka/KafkaRequestClient.cs`, `microservice_gateway/README.md`, `microservice_gateway/STRUCTURE.md`: устранён startup crash-loop gateway при проблемах с Kafka reply inbox. Bootstrap `reply.gateway.{instanceId}` больше не валит весь ASP.NET host, если Kafka controller/topic-create временно недоступны: gateway остаётся доступен по `/health`, а Kafka-facing requests ждут ready-state inbox в пределах своего timeout.
- `microservice_gateway/src/GatewayService.API/Kafka/IKafkaRequestClient.cs`, `microservice_gateway/src/GatewayService.API/Extensions/ServiceCollectionExtensions.cs`, `microservice_gateway/src/GatewayService.API/Market/DataServiceClient.cs`, `microservice_gateway/src/GatewayService.API/Market/DataTopics.cs`, `microservice_gateway/tests/GatewayService.UnitTests/Market/DataServiceClientTests.cs`, `microservice_gateway/API.md`, `microservice_gateway/README.md`, `microservice_gateway/STRUCTURE.md`: chart-triggered lazy hydrate больше не вызывает прямой `cmd.data.dataset.ingest`. Gateway теперь заводит queued ingest job через `cmd.data.dataset.jobs.start` и ждёт terminal status через `cmd.data.dataset.jobs.get`, поэтому first-hit chart hydrate использует тот же data-service ingest queue (cap 4) и per-table serialization по `target_table`. Hot-window cache `(symbol, timeframe, limit)` сохранён как fast-path для frequent latest-window requests.
- `microservice_admin/docker-compose.yml`, `microservice_admin/nginx/admin-online.conf.template`, `microservice_admin/.env.example`, `microservicestarter/start.sh`, `microservicestarter/restart.sh`, `microservicestarter/start.ps1`, `microservicestarter/restart.ps1`, root `README.md`, root `STRUCTURE.md`, `microservice_admin/README.md`, `microservice_admin/STRUCTURE.md`, `microservicestarter/README.md`, `microservicestarter/STRUCTURE.md`: `onlyadmin` переведён с прямого plain-HTTP publish `443:3000` на отдельный browser-facing TLS proxy. Теперь online-head стартует как `admin-online` + `admin-online-proxy`: `80` делает redirect на HTTPS, `443` обслуживает `/admin/*`, по умолчанию использует домены `sha-trade.tech` / `www.sha-trade.tech` и монтирует Let's Encrypt cert/key из `/etc/letsencrypt/live/sha-trade.tech/fullchain.pem` и `/etc/letsencrypt/live/sha-trade.tech/privkey.pem`.
- `microservice_gateway/src/GatewayService.API/Market/ChartService.cs`, `IDataServiceClient.cs`, `DataServiceClient.cs`, `microservice_gateway/README.md`, `microservice_gateway/API.md`, `microservice_gateway/STRUCTURE.md`: chart API для валидного symbol/timeframe теперь делает synchronous lazy hydrate missing window через `cmd.data.dataset.ingest` и повторно читает rows в том же HTTP-запросе. `pending`/`partial` остаются только fallback-сценариями, если ingest уже выполняется другим запросом, завершился ошибкой/timeout или data-service вернул `claim_check`.
- `microservice_admin/src/components/Sidebar.tsx`, `src/app/logs/page.tsx`, `src/app/api/logs/route.ts`, `src/lib/adminRuntimeLog.ts`: в админку добавлен шестой пункт меню **Logs**. Страница показывает process-local runtime diagnostics из admin (`/api/health`, `/api/kafka`, `backendClient.ts`), умеет запускать быстрый health/data/analytics check и не сохраняет token/payload.
- `.gitignore`: добавлены исключения для `microservice_admin/src/app/logs/**` и `microservice_admin/src/app/api/logs/**`, потому что общий шаблон `logs/` скрывал Next.js Logs routes из git и приводил к `404` на server deploy при формально одинаковом commit hash.
- `microservice_admin/src/hooks/useDatasetJobs.ts`, `microservice_admin/src/app/download/page.tsx`, `microservice_admin/README.md`, `microservice_admin/STRUCTURE.md`: Download-экран dataset jobs получил fallback polling `cmd.data.dataset.jobs.list` + `cmd.data.dataset.jobs.get` каждые 5 с для tracked ingest jobs. Это устраняет ложный UI-сценарий, когда backend уже перевёл job в `running`/terminal-state, но admin пропустил SSE progress/completed event и продолжал показывать `queued` / `stalled`.
- `microservice_data/src/DataService.API/Jobs/DatasetJobRunner.cs`, `microservice_data/README.md`, `microservice_data/STRUCTURE.md`: ingest concurrency поднят с 2 до 4. Heavy-timeframe guard (`1m`/`3m`) оставлен отдельным single-slot ограничителем, чтобы не разгонять самые тяжёлые TF параллельно.
- `microservice_admin/src/hooks/useDatasetJobsFeed.ts`, `microservice_admin/src/app/queue/page.tsx`, `microservice_admin/src/app/download/page.tsx`, `microservice_admin/src/components/Sidebar.tsx`, `microservice_admin/src/lib/i18n.ts`, `microservice_admin/README.md`, `microservice_admin/STRUCTURE.md`: добавлена 7-я вкладка **Queue**. Она объединяет live dataset jobs и request-stream из `/api/logs`; верхний `Active dataset jobs` panel убран с Download, а `AllIngestProgress` теперь выровнен на 4 execution slot-а, чтобы UI не расходился с backend runner.
- `microservice_admin/src/hooks/useDatasetJobs.ts`, `microservice_admin/src/app/api/kafka/route.ts`, `microservice_admin/src/app/queue/page.tsx`, `microservice_admin/README.md`, `microservice_admin/STRUCTURE.md`: Queue history отфильтрована от ping/read-only polling шума и теперь показывает только крупные операции с кратким payload/response summary. `refreshActiveJobs()` дополнительно доразрешает локально-зависшие active jobs через `cmd.data.dataset.jobs.get`, если completed-event был пропущен, поэтому finished job больше не должна висеть в active до перезагрузки страницы.

### 2026-05-18

- `microservice_data/src/DataService.API/Kafka/Topics.cs`, `src/DataService.API/Kafka/KafkaConsumerService.cs`, `microservice_analitic/backend/dataset/repair.py`, `microservice_analitic/backend/data_client.py`, `microservice_analitic/tests/test_repair.py`, `microservice_admin/src/app/download/page.tsx`, `microservice_data/README.md`, `microservice_data/STRUCTURE.md`, `microservice_analitic/README.md`, `microservice_analitic/STRUCTURE.md`, `microservice_admin/README.md`, `microservice_admin/STRUCTURE.md`: снят последний Bybit-only guard с Dataset repair. Analytics больше не дублирует market fetch в Python: `cmd.analitic.dataset.load_ohlcv` делегирует новый `cmd.data.dataset.repair_ohlcv` в data-service, который сам выполняет exchange-aware OHLCV repair для `bybit` / `binance` / `kraken` и публикует прежний `events.analitic.dataset.repair.progress`. `recompute_features` теперь тоже резолвит exchange-aware table name, а Download разрешает repair для Binance/Kraken. Добавлены focused pytest-тесты на делегирование и exchange-aware table resolution.
- `microservicestarter/start.sh`, `microservicestarter/restart.sh`, `microservicestarter/start.ps1`, `microservicestarter/restart.ps1`: launcher закреплён за одним split deployment path через backend HTTPS facade и собирает `ONLINE_*`, `ADMIN_BACKEND_BASE_URL`, `PUBLIC_DOWNLOAD_BASE_URL`, `ADMIN_BACKEND_PORT`; прежняя token-sync часть позднее удалена.
- Те же launcher-скрипты: token-sync схема между backend-host и admin-host позднее заменена на login-only admin account и JWT session.
- `microservicestarter/start.sh`, `microservicestarter/restart.sh`: bash-ветка parallel start/restart теперь включает список конкретных упавших сервисов в итоговой ошибке, а не только общий failure summary.
- `microservicestarter/start.sh`, `microservicestarter/restart.sh`, `microservicestarter/start.ps1`, `microservicestarter/restart.ps1`: launcher теперь автоматически восстанавливает отсутствующий `.env` из `.env.example` во время запуска/рестарта и больше не делает конкурентный `docker image prune` в child-процессах parallel fan-out.
- `microservice_analitic/docker-compose.yml`: internal Redis больше не публикуется на host `6379`, чтобы backend auto-deploy не падал на port conflict; launcher cleanup дополнительно защищён межпроцессным lock-ом вокруг `docker image prune`.
- `microservice_admin/docker-compose.yml`, `microservice_infra/docker-compose.yml`, `microservice_admin/.env.example`: удалены legacy split-transport сервисы и переменные. Split deployment больше не документируется и не поддерживается через join-token transport.
- `README.md`, `STRUCTURE.md`, `microservicestarter/README.md`, `microservicestarter/STRUCTURE.md`, `microservice_admin/README.md`, `microservice_admin/STRUCTURE.md`, `microservice_infra/README.md`, `microservice_infra/STRUCTURE.md`: документация синхронизирована под один официальный маршрут backend `:8443`; auth позднее переведён с общего ключа на admin JWT session.
- Два obsolete split-transport guide-файла удалены из `microservice_infra`.
- `microservice_admin/src/app/api/health/route.ts`: исправлен split-mode response `/api/health` для production typecheck. Route снова возвращает полный `KafkaBrokerHealth` с `bootstrapServers` и согласованными `online/offline` статусами; `npm run build` в `microservice_admin` проходит.
- `microservice_infra/docker-compose.yml`, `microservice_infra/nginx/cert-init.Dockerfile`, `microservice_infra/nginx/cert-init.sh`: backend split facade на `:8443` теперь сам bootstrap-ит TLS. One-shot `nginx-cert-init` генерирует self-signed `tls.crt`/`tls.key` в `ADMIN_BACKEND_CERTS_DIR`, если каталог пустой, и тем самым убирает crashloop `modelline-nginx` на чистом backend-host.
- `microservice_admin/docker-compose.yml`, `microservice_admin/.env.example`, `microservice_admin/src/lib/backendClient.ts`: добавлен `ADMIN_BACKEND_TLS_INSECURE` для split deployment. `admin-online` по умолчанию запускается в self-signed-friendly режиме (`1`) и принимает autogenerated backend cert; trusted-cert сценарий остаётся через явный `0`.
- `microservice_infra/.env.example`, `microservice_admin/.env.example`, `microservice_account/.env.example`, `microservice_gateway/.env.example`, `README.md`, `microservice_infra/README.md`: убраны оставшиеся legacy transport-упоминания. Актуальный split deployment теперь документируется только через backend HTTPS facade `:8443` и обычные private/public IP или DNS.
- `microservicestarter/start.sh`, `microservicestarter/restart.sh`, `microservicestarter/README.md`, `microservicestarter/STRUCTURE.md`, root `README.md`, root `STRUCTURE.md`: bash launcher теперь делает preflight host-port checks перед `docker compose up` и раньше времени ловит конфликты с внешними контейнерами/процессами. `stop.sh`/`stop.ps1` явно задокументированы как lifecycle-инструмент только для compose-стеков ModelLine, а не для сторонних контейнеров на хосте.
- `microservice_infra/nginx/nginx.conf`, `microservice_infra/README.md`, `microservice_infra/STRUCTURE.md`: исправлен upstream split admin facade с `gateway:5020` на реальный docker alias `gateway-service:5020`. Это убирает ложный `502 Bad Gateway` на `https://<backend>:8443/api/admin/*`, когда gateway уже здоров на прямом порту `7520`.
- `microservicestarter/start.sh`, `microservicestarter/restart.sh`, `microservicestarter/README.md`, `microservicestarter/STRUCTURE.md`: исправлена ложная классификация собственных контейнеров compose-проекта как внешних при preflight port check. `restart` снова должен работать без ручного `stop` в CI/CD, если publish-порт уже занят текущим контейнером того же сервиса.
- `microservicestarter/start.sh`, `microservicestarter/restart.sh`, `microservicestarter/README.md`, `microservicestarter/STRUCTURE.md`: preflight parser теперь распознаёт Docker publish ranges вроде `9000-9001->9000-9001`, чтобы собственный MinIO не определялся как сторонний `docker-proxy` на повторном `restart`.
- `microservice_gateway/src/GatewayService.API/DTOs/ErrorResponse.cs`, `microservice_admin/src/lib/backendClient.ts`, `microservice_admin/src/app/api/kafka/route.ts`: split admin facade получил структурированную диагностику auth/transport ошибок; актуальная схема нормализует missing/expired/non-admin session, `502`, `504` и network failures для UI.
- `microservice_gateway/src/GatewayService.API/Kafka/KafkaRequestClient.cs`, `microservice_gateway/README.md`, `microservice_gateway/STRUCTURE.md`: gateway теперь явно создаёт per-instance reply topic `reply.gateway.{instanceId}` перед подпиской consumer. Это адресует live-сценарий, где backend token уже валиден, но `/api/admin/health/*` уходит в `504 Kafka request timed out` из-за неготового reply-inbox path.
- `microservice_admin/src/lib/backendClient.ts`, `microservice_admin/src/app/api/health/route.ts`, `microservicestarter/start.*`, `microservicestarter/restart.*`: admin split diagnostics теперь различает self-signed TLS failure как `admin_backend_tls_untrusted` с `err.cause`, `/api/health` берёт `ADMIN_BACKEND_BASE_URL` из runtime env на каждый запрос, а launcher записывает `ADMIN_BACKEND_TLS_INSECURE=1` для пустого `https://` backend facade, чтобы admin-host принимал autogenerated backend cert.
- `microservice_admin/src/app/api/health/route.ts`, `microservice_admin/src/app/api/kafka/route.ts`, `microservice_admin/src/lib/backendClient.ts`, `microservice_gateway/src/GatewayService.API/Controllers/AdminController.cs`, `microservice_gateway/src/GatewayService.API/Kafka/KafkaRequestClient.cs`: добавлены подробные runtime logs для split connectivity troubleshooting без печати token/payload. Admin пишет `[api/health]`, `[api/kafka]`, `[admin-backend]`; gateway пишет `AdminFacade request ...` и `KafkaRequest ...`, все этапы связываются через `correlationId`.
